# План: резервное копирование Postgres

Составлен 2026-07-30. Закрывает пункт «Бэкапов нет вообще» из `mainnet-cutover-plan.md`
(фаза 0) — **делается ДО перехода на реальные деньги**.

## Статус (2026-08-02)

Код в `main` (`aa44188`) и на проде. Сделано: `age`+`rclone` на сервере, пара
ключей (приватный — только на машине владельца, `%USERPROFILE%\tgp-backup.key`,
**перенести в менеджер паролей**), публичный в `/opt/tg-poker/.env`, каталог
`backups/`, скрипт `backup.sh` + `scripts/tgp-backup.cron` + `scripts/restore-check.sh`.
Проверено живьём: `pg_dump | age` даёт 102 КБ шифротекста, приватный ключ
расшифровывает его локально в валидный архив (`PGDMP`).

Канал алертов подключён: `BACKUP_TG_CHAT_ID=158394554`, тестовое сообщение дошло.

**Осталось (ждёт владельца):** ключ Backblaze B2 → `rclone config` → ручной
прогон → крон → полный учебный restore (§5, нужен запущенный Docker Desktop).
Крон намеренно НЕ ставим до настройки rclone — иначе каждый час алерт.

## Решения (согласованы с владельцем 2026-07-30)

- **Основной внешний канал:** ~~Cloudflare R2~~ → **Backblaze B2** (замена 2026-08-02).
  Cloudflare требует привязать карту даже под бесплатные 10 ГБ; отдавать реквизиты
  ради 27 МБ шифротекста незачем. B2 — то же самое по сути (10 ГБ бесплатно,
  S3-совместимо, rclone поддерживает нативным бэкендом `b2`), карта при регистрации
  не нужна. Для скрипта смена провайдера — одна строка `BACKUP_REMOTE` в `.env`.
- **Частота:** дамп каждый час; каждый дамп сразу уходит на удалённое хранилище.
- **Дубль:** раз в сутки зашифрованный дамп телеграм-ботом в личку владельца.
- **Шифрование:** `age` с асимметричной парой ключей — на сервере лежит только
  **публичный** ключ (шифрование), приватный (расшифровка) хранится у владельца вне сервера.
  Утечка любого хранилища бэкапов без приватного ключа бесполезна.

## Схема

```
cron (хост, ежечасно, minute=17)
  └─ /opt/tg-poker/backup.sh
       1. docker compose exec -T postgres pg_dump -U poker -Fc poker_db
          → db-YYYYMMDD-HHMM.dump          (консистентный снапшот, app не останавливается)
       2. age -r <pubkey> → db-....dump.age (оригинал не сохраняется)
       3. локально: /opt/tg-poker/backups/, ретеншен 48 часов
       4. rclone copy → b2:tgp-backups/hourly/
       5. если час == 03 UTC:
            копия → b2:tgp-backups/daily/
            + sendDocument ботом в личку владельца
       6. ретеншен на удалённом: rclone delete --min-age 7d/90d
       7. любая ошибка шагов 1–6 → sendMessage ботом владельцу (алерт)
```

Ретеншен на удалённом хранилище — **скриптом** (правка 2026-08-02; изначально
планировались lifecycle-правила бакета): `hourly/` — 7 дней, `daily/` — 90.
Почему перенесли: одним элементом конфигурации в чужом веб-интерфейсе меньше,
видно в логе, переживает смену провайдера. Шаг 6 идёт последним осознанно —
к этому моменту бэкап уже сохранён, так что падение ретеншена сигналит о росте
хранилища, но никогда не стоит нам бэкапа. `RCLONE_B2_HARD_DELETE=true` —
иначе B2 на `delete` только вешает hide-маркер, версия остаётся, и бакет пухнет
при внешне аккуратной выдаче `rclone ls`.

**Объёмы:** план исходил из «дамп < 5 МБ»; по факту **102 КБ**. Локально
48 × 102 КБ ≈ 5 МБ; на удалённом (168 hourly + 90 daily) × 102 КБ ≈ **27 МБ**
против 10 бесплатных ГБ. Квота перестала быть фактором выбора провайдера.

## Шаги реализации

### 0. Входные данные от владельца
- [ ] ~~Аккаунт Cloudflare → R2~~ (требует карту) → **Backblaze B2**: создать бакет
      `tgp-backups` (имена в B2 глобально уникальны — если занято, взять с суффиксом
      и записать в `BACKUP_REMOTE`), затем Application Key, скоупленный на этот бакет
      → `keyID` + `applicationKey`.
- [x] Telegram chat id владельца для дублей и алертов (получим через `getUpdates`
      после `/start` боту — вебхук на боте не настроен, конфликта нет). → `158394554`.
- [ ] Решить, где живёт приватный ключ `age` (менеджер паролей; можно + бумажная копия).
      ⚠️ **Потеря приватного ключа = все бэкапы превращаются в мусор.**

### 1. Подготовка сервера (один раз)
- [x] `apt install age` (1.0.0), `curl https://rclone.org/install.sh | bash` (1.75.0).
- [x] Пара ключей. **Отступление от плана:** генерировали не на сервере, а локально
      (`winget install FiloSottile.age`) — приватный ключ тогда вообще ни разу не
      оказывается на машине, от компрометации которой он защищает, и `shred` не нужен.
      Публичная строка — в `/opt/tg-poker/.env` (`BACKUP_AGE_RECIPIENT=age13kmug4…`),
      приватный лежит в `%USERPROFILE%\tgp-backup.key` (перенести в менеджер паролей).
- [ ] `rclone config create b2 b2 account=<keyID> key=<applicationKey>`; проверить
      `rclone lsd b2:`. Права: `chmod 600 ~/.config/rclone/rclone.conf`.
- [x] В `.env` добавить `BACKUP_TG_CHAT_ID=158394554` (BOT_TOKEN там уже есть; бот —
      @Testpoke_bot, **при переезде на боевого бота алерты уедут вместе с токеном**).
      Compose-списка это не касается — скрипт работает на хосте и читает `.env` сам.
      Тестовое sendMessage тем же путём, каким шлёт алерт `backup.sh`: доставлено.
- [x] `mkdir -p /opt/tg-poker/backups && chmod 700 /opt/tg-poker/backups`.
- [x] Каталог в `.gitignore` (репозиторий и есть `/opt/tg-poker` — иначе дампы
      всплывут в `git status`). Заодно `*.key`.

### 2. Скрипт `/opt/tg-poker/backup.sh` (в репозитории)
- [x] Всё из схемы выше, плюс:
      `set -euo pipefail`; `flock` на lock-файл (защита от наложения запусков);
      лог в `/opt/tg-poker/backups/backup.log` (с ротацией по размеру);
      `trap` на ошибку → sendMessage-алерт с текстом упавшего шага;
      имя сервиса БД — `postgres` (не `db`).
- [x] Проверка «дамп не пустой»: **размер > 10 КБ у шифротекста**, а не у плейнтекста.
      Причина отступления: чтобы померить плейнтекст, его пришлось бы сначала положить
      на диск — ровно то, что раздел «сознательно НЕ делаем» и запрещает. `age` не
      сжимает и не паддит (`pg_dump -Fc` уже сжат), так что шифротекст повторяет размер
      исходника с точностью до ~200-байтового заголовка, и сценарий «Postgres лежит,
      cron бодро бэкапит нули» ловится так же. Сейчас реальный дамп — 102 КБ.
      Дополнительно: `pipefail` роняет прогон, если `pg_dump` умер посреди потока
      (`age` при этом честно шифрует обрубок и возвращает 0).
- [x] Прогон `pg_dump | age` на проде: 102 098 байт, заголовок `age-encryption.org/v1`.
- [x] Приватный ключ проверен на настоящем дампе: `age -d` → валидный `PGDMP`.

### 3. Крон (хост)
- [ ] `install -m 644 /opt/tg-poker/scripts/tgp-backup.cron /etc/cron.d/tgp-backup`
      (файл готов в репозитории). Минута 17 — чтобы не совпадать с деплоями и чужими
      «ровно в час» задачами. Вызов через `/bin/bash <путь>`, чтобы не зависеть от
      бита `+x`, переживающего чекаут с Windows.
      **Ставить только после `rclone config`** — иначе каждый час алерт о падении.

### 4. ~~Lifecycle-правила R2~~ → ретеншен в скрипте
- [x] Пункт снят вместе с переездом на B2: `rclone delete --min-age 7d|90d` в шаге 6
      `backup.sh`, настройки `BACKUP_REMOTE_HOURLY_DAYS` / `BACKUP_REMOTE_DAILY_DAYS`.
      В веб-интерфейсе бакета настраивать нечего.

### 5. Проверка восстановления (обязательная, до вычёркивания пункта из mainnet-плана)
- [ ] Скачать свежий `.age` с удалённого хранилища **на другую машину** (локальную), расшифровать
      приватным ключом, поднять чистый `postgres:16-alpine`, `pg_restore -d poker_db`.
- [ ] Сверить: `count(*)` по `users` и `transactions`, сумма `balance`, последняя
      строка леджера — совпадают с продом на момент дампа.
- [ ] Записать фактическое время процедуры (это RTO) в этот файл.
- [ ] Повторять после крупных миграций схемы; иначе — раз в квартал.

### 6. Мониторинг тишины
- [ ] Алерт срабатывает при ошибке, но не при «cron вообще не запустился».
      Дешёвое решение: суточный ТГ-дубль и есть heartbeat — **нет утреннего файла
      в личке = бэкапы стоят**, разбираться. Зафиксировать это как договорённость
      (владелец «подписан» на утренний файл).

## Что сознательно НЕ делаем (и почему)

- **WAL-архивация / PITR / реплика** — на базе в мегабайты это инфраструктура ради
  инфраструктуры; почасовой дамп покрывает риск. Вернуться к вопросу, когда дамп
  перевалит за сотни МБ или час потерь станет недопустим.
- **Бэкап тома `pgdata` целиком** — `pg_dump` переносим между минорными версиями и
  восстанавливается выборочно; сырой том — нет.
- **Хранение нешифрованных дампов где-либо** — включая локальный каталог.

## Восстановление (шпаргалка на чёрный день)

```bash
# на новом/том же сервере, postgres поднят, база пустая
rclone copy b2:tgp-backups/hourly/<последний>.dump.age .
age -d -i tgp-backup.key <файл>.dump.age > restore.dump
docker compose -f docker-compose.prod.yml cp restore.dump postgres:/tmp/r.dump
docker compose -f docker-compose.prod.yml exec -T postgres \
  pg_restore -U poker -d poker_db --clean --if-exists /tmp/r.dump
# затем: сверить money invariant в админке; фишки за столами на момент дампа
# потеряны (жили в памяти) — инвариант покажет ровно этот разрыв.
```

⚠️ Нюанс восстановления: стеки сидевших игроков живут в памяти `TableManager`, в дампе
их нет — после восстановления инвариант покажет недостачу размером «фишки в игре на
момент дампа». Это ожидаемо; спорные случаи разбирать по `HandHistory` + session JSONL.
