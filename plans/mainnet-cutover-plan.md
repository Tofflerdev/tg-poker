# План: запуск боевого сервера (mainnet, реальные деньги)

Составлен 2026-07-25 по итогам полного прогона фаз 1–6 плана `crypto-payments-rake-plan.md`
на testnet. **Переработан 2026-08-04**: решено поднимать боевой прод на **отдельной машине**,
а текущий сервер оставить тестовым стендом. До этого план описывал переключение токенов
на месте — та версия в истории git, если понадобится.

## Топология после запуска

| | Боевой прод | Тестовый стенд (нынешний сервер) |
|---|---|---|
| Машина | `nightriver.isgood.host` (с 2026-08-11) | `tgp.isgood.host` |
| Crypto Pay | «Night River App» @CryptoBot (с 2026-08-12) | testnet «Buckram Wisent App» |
| Бот | `@NightRiverPoker_bot` (с 2026-08-12) | `Testpoke_bot` |
| БД | **пустая с нуля** | как есть |
| `SENTRY_ENVIRONMENT` | `production` | `staging` |
| Домен | `nightriver.isgood.host` | `tgp.isgood.host` (остаётся) |
| Аккаунты игроков | **чистый старт**, ничего не переносим | остаются как есть |

**Главное следствие новой схемы:** чистка экономики (в прежней версии — самая опасная фаза)
**отпадает**. Боевая база стартует пустой, минтить нечего, `MONEY_INVARIANT_BASELINE_CHIPS`
естественным образом `0`. Testnet-фишки остаются на стенде, где им и место.

**Взамен появляется риск другого рода:** боевую машину нужно собрать с нуля, ничего не забыв —
SSL, бэкапы, крон, переменные. Забытый бэкап на боевой базе хуже, чем забытый на стенде.

---

## Фаза 0. Предусловия (сделать ЗАРАНЕЕ, не в день запуска)

- [x] ~~**Выяснить позицию Crypto Pay по покеру на реальные деньги.**~~ **Закрыто 2026-08-10
      по прецеденту:** Crypto Pay принимает депозиты для TONPoker, то есть покерная тематика
      сама по себе провайдера не смущает. Отдельный запрос в поддержку не делаем.
      Что прецедент **не** покрывает: он наблюдается на депозитном плече (`createInvoice`).
      Выплаты идут через `transfer`, который включается вручную по каждому приложению —
      остаток вопроса снимается в фазе B (включат или нет) и в фазе E (свой вывод $10),
      то есть до того, как внутри окажутся деньги игроков.
      ⚠️ **Отсюда — порядок открытия:** не пускать игроков вносить депозиты, пока свой
      вывод в фазе E не прошёл. Иначе провал на `transfer` застаёт приложение с чужим
      флоатом внутри — ровно тот сценарий, ради которого пункт и заводился.
- [x] ~~**Настроить бэкапы Postgres.**~~ **Сделано 2026-08-02** на нынешнем сервере
      (`plans/db-backup-plan.md`): почасовой `pg_dump`, шифрование `age`, Backblaze B2 +
      суточный дубль в личку, ретеншен 48 ч / 7 дней / 90 дней, восстановление проверено.
      ⚠️ На боевой машине всё это придётся поднять **заново** — см. фазу A.
- [x] ~~**Завести Sentry и прописать `SENTRY_DSN`.**~~ **Сделано 2026-08-04.** Проект на
      sentry.io (план Developer, EU), DSN в `.env`, алерт инварианта проверен сквозным
      событием: issue пришёл, письмо дошло, персональные данные скраббер вырезал.
      Коммитами `513e993`/`b57d288`/`70fd982` добавлены проброс переменных в compose,
      `Sentry.setupExpressErrorHandler`, тег релиза и `SENTRY_ENVIRONMENT`.
      **2026-08-11: стенд помечен `SENTRY_ENVIRONMENT=staging`** — до этого переменной
      в его `.env` не было вовсе, тег падал на `NODE_ENV=production`, и стенд в Sentry
      был неотличим от боевого (привязывать правило было не к чему). Проверено внутри
      контейнера: `printenv SENTRY_ENVIRONMENT` → `staging`.
      **Правило алертов ограничено окружением `production` 2026-08-11** (сделано
      владельцем в UI Sentry). Остаётся только `SENTRY_ENVIRONMENT=production`
      на боевой машине — это уже в таблице фазы C.
      ⚠️ Старые issue, записанные стендом под `production` до 2026-08-11, так и
      останутся в этом окружении — фильтр действует только на новые события.
- [ ] **Пароли — генерировать на боевой машине с нуля.** На стенде `ADMIN_PASS` и
      `POSTGRES_PASSWORD` совпадают (сверено 2026-08-02). Разводить их там смысла нет —
      машина станет тестовой. **Но `.env` со стенда на боевую машину не копировать**,
      иначе совпадение переедет туда, где оно уже стоит реальных денег. Каждый секрет
      боевой машины генерируется отдельно:
      `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`.
- [x] ~~**Юридическая часть.**~~ **Тексты переписаны 2026-08-11** (`98ef78c`, на стенде).
      Старая редакция описывала play-money приложение — «фишки виртуальны и не имеют
      денежной ценности», «не является игорным сервисом», «нет депозитов и выводов»,
      экономика на ежедневном бонусе, которого нет в коде с фазы §G. Все три страницы
      переписаны по фактическому коду: порядок депозитов, комиссия Crypto Pay, рейк
      с кэпами по столам, лимиты и ручное одобрение выводов, реальный состав данных
      в Privacy. Версия поднята 1.0 → 2.0 (`client/src/pages/legal/legalMeta.ts`) —
      она хранится в `User.tosVersion`, и согласие с play-money текстом боевой продукт
      не покрывает. Гейт сравнивает только `tosAcceptedAt IS NULL`, версии не сверяет;
      на боевой пустой базе это неважно, на стенде старые аккаунты 2.0 не увидят.
      ⚠️ **Три обещания в тексте держатся руками, а не кодом** — до роста аудитории
      это допустимо, но знать их надо до того, как о них попросят:
      самоисключение по запросу (едет на `banUser`, срок нигде не хранится),
      «обычно в течение 24 часов» на вывод (SLA в коде нет, заявка ждёт админку),
      удаление аккаунта (хендлера нет, ручной SQL).
      Вне компетенции разработки остаётся юридическая вычитка: формулировок,
      требующих юрлица и юрисдикции, в текстах намеренно нет.

## Фаза A. Провижининг боевой машины

Пошаговая инструкция — `DEPLOY.md`, раздел «First-time setup (new VPS)». Здесь только то,
что специфично для боевого запуска и чего в `DEPLOY.md` нет.

### A1. Домен — ✅ ЗАКРЫТО 2026-08-05

**`tgp.isgood.host` остаётся за тестовым стендом. Боевая машина получает новый домен.**
Ничего не перевешиваем: A-запись стенда, его сертификат и URL тестового бота не трогаем.

**Домен параметризован и выкачен на стенд** (`2633569`, `37e0b9c`). Для боевой машины
теперь достаточно `DOMAIN=<новый домен>` в её `.env` — `server_name`, пути к сертификату
и CORS подтянутся сами, правок в коде не требуется. Остаётся только **выбрать** имя.

Ниже — что именно было не так и как сделано.

Обе машины деплоятся из одного репозитория и одного `main`, а домен был зашит в код
и в конфиг, а не в `.env`:

- `server/index.ts:108-109` — в проде `CORS_ORIGIN` это литерал `["https://tgp.isgood.host"]`.
  На боевой машине сокеты и `/api/admin/login` с нового домена получат отказ CORS.
- `nginx/nginx.conf:7,21,24,25` — `server_name` и пути к сертификату Let's Encrypt,
  которые содержат имя домена.

Пока домен был один на всё, литералы работали. С двумя машинами один и тот же файл
должен обслуживать два имени — значит домен нужно параметризовать **до** провижининга:

- [x] **`CORS_ORIGIN` из окружения.** Сделано `2633569`: список через запятую, в compose
      по умолчанию `https://${DOMAIN}`, при старте пишется `[Boot] CORS allowlist: …`.
- [x] **`nginx.conf` → шаблон.** Образ `nginx:alpine` сам прогоняет `envsubst` по
      `/etc/nginx/templates/*.template` и кладёт результат в `conf.d/`. Переименовать в
      `nginx/default.conf.template`, монтировать **каталог** `./nginx` в
      `/etc/nginx/templates`, подставлять `${DOMAIN}`.
      `NGINX_ENVSUBST_FILTER: DOMAIN` — сузить подстановку до одного имени.
      Переменные самого nginx (`$host`, `$uri`, `$remote_addr`) уцелеют и без фильтра:
      скрипт образа передаёт `envsubst` явный список имён переменных окружения,
      а `envsubst` со списком другие `$...` не трогает. Фильтр нужен на случай
      переменной окружения, названной как переменная nginx (строчными).
- [x] Проверить **на стенде**: с `DOMAIN=tgp.isgood.host` итоговый конфиг обязан совпасть
      с нынешним. **Сделано 2026-08-05** — совпал побайтово по директивам; после деплоя
      на стенде: 200 по HTTPS, `/api/tables` отвечает, 301 с HTTP, HSTS и некэшируемый
      `index.html` на месте, `[Boot] CORS allowlist: https://tgp.isgood.host`. `docker compose exec nginx cat /etc/nginx/conf.d/default.conf` —
      сравнивать то, что **внутри контейнера**, а не на хосте.
      Локально то же самое проверяется без деплоя:
      `docker run --rm -e DOMAIN=... -e NGINX_ENVSUBST_FILTER=DOMAIN -v ./nginx:/etc/nginx/templates:ro
      --entrypoint /bin/sh nginx:alpine -c "/docker-entrypoint.d/20-envsubst-on-templates.sh; cat /etc/nginx/conf.d/default.conf"`.
      ⚠️ Штатный entrypoint запускает init-скрипты, **только если команда — `nginx`**;
      с любой другой командой шаблоны молча не обработаются, и вы получите стоковый
      конфиг образа, приняв его за свой.

Побочная выгода: монтирование каталога вместо одиночного файла снимает грабли с inode —
`git pull` больше не подменяет файл под работающим контейнером.

### A2. Развернуть приложение — ✅ ВЫПОЛНЕНО 2026-08-11

**Домен: `nightriver.isgood.host`** (1 vCPU / 2 ГБ / 40 ГБ, Ubuntu 26.04 `resolute`).
Boot-лог показал `CORS allowlist: https://nightriver.isgood.host`, Sentry поднялся,
инвариант запущен, Crypto Pay ожидаемо выключен. HTTPS 200, HTTP → 301, HSTS на месте,
в базе 4 таблицы и 2 системных аккаунта (house + bot bankroll), транзакций ноль.

- [x] VPS, Docker, клон репозитория, сертификат + renewal-hooks — всё по `DEPLOY.md`.
      Отклонения, сделанные осознанно: `rclone` из репозитория Ubuntu вместо
      `curl | bash` от root; certbot зарегистрирован на почту владельца (на стенде —
      без email), чтобы уведомление об истечении сертификата было куда прислать.
      Swap 2 ГБ заведён отдельно — в `DEPLOY.md` его не было, а на 1 vCPU/2 ГБ он
      страхует пик сборки.
- [x] ~~`.env` **писать руками**, не копировать со стенда (см. фазу 0).~~ **Дописан
      2026-08-12**, заглушек не осталось. Полный список ключей — фаза C; заготовка
      с плейсхолдерами и предупреждениями — `.env.production.example`
      (`cp .env.production.example .env`). ⚠️ Прежний `cp .env.production .env` из
      `DEPLOY.md` был нерабочим: файл в `.gitignore` и на свежем клоне отсутствует —
      исправлено 2026-08-11 вместе с ревизией `DEPLOY.md`.
- [x] `docker compose -f docker-compose.prod.yml up -d --build`, дождаться `Healthy`.
      Собрано через `update.sh` (он же проставляет `SENTRY_RELEASE`); все три контейнера
      healthy, диск после сборки 8.7 ГБ из 40.
- [x] ~~⚠️ **Осталось от фазы B:** заглушки `BOT_TOKEN` / `BOT_USERNAME`.~~
      **Закрыто 2026-08-12:** вписаны боевые `CRYPTO_PAY_TOKEN` (42 симв.),
      `BOT_TOKEN` (46 симв.) и `BOT_USERNAME=NightRiverPoker_bot`; строк
      `PENDING_PHASE_B` в `.env` больше нет. Промежуточное состояние (плейсхолдер
      вместо пустого `BOT_TOKEN`, чтобы не сработал `assertSafeBootOrExit`)
      прожило ровно сутки.

### A3. Бэкапы на боевой машине

Без них боевая база живёт без страховки — это хуже, чем отсутствие бэкапов на стенде,
с которого нечего терять.

- [x] `age`, `rclone`, конфиг B2, `install -m 644 scripts/tgp-backup.cron /etc/cron.d/tgp-backup` —
      **сделано 2026-08-11.** Конфиг rclone перенесён со стенда (учётные данные B2 общие).
- [x] `BACKUP_AGE_RECIPIENT` — **тот же публичный ключ**, приватный по-прежнему только
      у владельца. Заводить вторую пару ключей незачем: потерять один ключ проще, чем два.
- [x] ⚠️ **`BACKUP_REMOTE` обязан отличаться от стендового.** `b2:tgp-backups/prod`
      на боевой. **Опасение подтвердилось на практике:** обе машины 2026-08-11 в 17:17
      создали файл `db-20260811-1717.dump.age`, и при дефолтном префиксе боевой дамп
      (11 КБ, пустая база) затёр бы стендовый (101 КБ) на первом же прогоне.
      ⚠️ ~~**Уточнение к фазе F:** стенд пишет не в `staging`, а в корень бакета.~~
      **Разрешено 2026-08-12 в фазе F:** файлы стенда перенесены `rclone move` из
      корня в `staging/`, `BACKUP_REMOTE` переключен. В корне остались только
      `prod/` и `staging/`.
- [x] Прогнать `bash backup.sh` руками и убедиться, что файл лёг в **свой** префикс.
      Прошло: дамп 11 646 байт → `b2:tgp-backups/prod/hourly/`, файлы стенда целы.
- [ ] Проверить восстановление с боевой машины: `scripts/restore-check.sh <file.age> <key>`.
      **Требует приватного ключа `age`, который лежит у владельца вне сервера** — Claude
      выполнить не может, делается вручную.
- [ ] ⚠️ **Телеграм-heartbeat бэкапов.** `backup.sh` шлёт суточный дамп и алерты об
      ошибках через `BOT_TOKEN`. С 2026-08-12 там боевой токен @NightRiverPoker_bot,
      то есть техническая часть готова — **но канал заработает только после того, как
      владелец сам напишет боту `/start`**: Telegram не даёт боту писать первым, и до
      открытого диалога отправка падает с `403 bot can't initiate conversation` при
      совершенно верном `BACKUP_TG_CHAT_ID`. Пункт закрывается не кодом, а первым
      дошедшим суточным дампом; до тех пор смотреть `backups/backup.log` руками.

### A4. Аккаунты игроков — РЕШЕНО 2026-08-05: чистый старт

**Ничего не переносим, боевая база стартует пустой.** Игроки заново вводят имя и принимают
ToS; на стенде их аккаунты остаются нетронутыми.

Что это даёт: **приложение с SQL чистки не нужно вовсе.** Ни дампов, ни восстановления,
ни остановки `app` ради обнуления балансов — то есть самый опасный сценарий прежнего плана
(фишки, обеспеченные testnet-USDT, превращаются в право вывести реальные деньги) в новой
схеме физически невозможен. `MONEY_INVARIANT_BASELINE_CHIPS=0` верен с первой секунды.

Единственная плата — тестировавшиеся игроки на боевом старте будут новыми пользователями.

## Фаза B. Боевое приложение Crypto Pay и бот

**ФАЗА ЗАКРЫТА 2026-08-12.** Боевое приложение Crypto Pay и боевой бот на месте,
`.env` дописан, приложение перезапущено и проверено. Открытым остаётся один пункт —
подтверждение вебхука со стороны CryptoBot, и он закрывается только первым депозитом
(см. ниже, почему смоук сам по себе не доказательство).

- [x] ~~Создать приложение в **@CryptoBot** (не @CryptoTestnetBot), сохранить токен.~~
      **Сделано 2026-08-12:** приложение **«Night River App»**. Токен вписан в `.env`
      боевой машины владельцем вручную (`read -s`, в чат и в историю shell не попадал).
      Boot-лог: `[Boot] Crypto Pay connected as app "Night River App"` — имя приложения
      и есть доказательство нужного токена, у стенда оно «Buckram Wisent App».
- [x] ~~**Включить метод `transfer`** в настройках ограничений приложения.~~
      **Включено и проверено 2026-08-12** пробой с машины: `transfer` вернул
      `400 INSUFFICIENT_FUNDS`, а не `403 METHOD_DISABLED`.
      ⚠️ Сама по себе такая проба ничего не доказывает — неизвестно, в каком порядке
      API проверяет права и средства. Доказательство даёт **контрольный** вызов:
      `createCheck` на том же токене отдал `METHOD_DISABLED`, то есть 403 приходит
      раньше проверки баланса, и `INSUFFICIENT_FUNDS` действительно означает «метод
      разрешён». Порядок «сначала сумма, потом права» тоже виден: на 1.0 USDT пришёл
      `AMOUNT_TOO_SMALL` с `min_send_amount_in_usd: 1` — минимум провайдера $1,
      граница не включительная. Наш порог вывода $10 выше, конфликта нет.
      ⚠️ Повторить пробу даром больше нельзя: она безопасна ровно потому, что кошелёк
      пуст (`getBalance` — нули по всем 11 активам). С деньгами внутри вызов `transfer`
      с валидными параметрами их переведёт.
- [x] ~~`createCheck` **не включать**~~ — **проверено 2026-08-12**: `403 METHOD_DISABLED`.
      Нужны ровно пять методов: `getMe`, `createInvoice`, `getInvoices`, `transfer`,
      `getTransfers`. Меньше включённых методов — меньше способов опустошить кошелёк
      при утечке токена. (`getTransfers` отдельного выключателя не имеет.)
- [ ] Webhook: `https://nightriver.isgood.host/api/crypto/webhook`.
      **Проверено наполовину 2026-08-12:** неподписанный `POST` снаружи отдаёт `401`,
      а до вписывания токена отдавал `503` — значит nginx доводит запрос до приложения
      и подпись проверяется. Чего это **не** доказывает: что CryptoBot настроен звать
      именно этот URL — со стороны API это не читается, только глазами в настройках
      приложения. ⚠️ Смоук фазы E сам по себе тоже не доказывает: при сломанном вебхуке
      депозит всё равно зачислится, его подберёт `startDepositReconciliation`, и поломка
      останется незамеченной до первой ситуации, где важна секундная реакция.
      Различать по времени строки `[Deposit] credited invoice …`: секунды после оплаты —
      вебхук, минуты — реконсиляция.
- [ ] Учесть, но ничего не делать: первые **30 дней** приложение не может прикладывать
      `comment` к переводу (`CANNOT_ATTACH_COMMENT`) — код повторяет перевод без
      комментария сам (`84da351`). Приложение создано 2026-08-12, то есть окно
      закрывается около **2026-09-11**: первые боевые выплаты уйдут без комментария,
      и это норма, а не сбой.
- [ ] Учесть, но ничего не делать: ставка комиссии на mainnet может отличаться от тестовых
      3% — она наблюдается из первого оплаченного инвойса автоматически
      (`server/payments/depositFee.ts`). На 2026-08-12 в логе
      `[Boot] Crypto Pay fee: 3.00% (built-in default)` — это встроенный дефолт,
      а не наблюдённая ставка: оплаченных инвойсов ещё нет. Первое, что нужно
      посмотреть после депозита $5 в фазе E.
- [x] ~~Создать/выбрать **боевого бота**, получить токен; в BotFather указать URL мини-аппа
      на боевой домен.~~ **Сделано 2026-08-12: @NightRiverPoker_bot** (id 8835120941,
      имя «Night River Poker»), URL мини-аппа → `https://nightriver.isgood.host`.
- [x] ~~⚠️ **`BOT_TOKEN` и бот меняются строго вместе.**~~ Токен вписан владельцем
      вручную (46 симв.) и **проверен с машины через Telegram `getMe`**: ответ называет
      `username: NightRiverPoker_bot` и `has_main_web_app: true`, то есть токен от нужного
      бота и URL мини-аппа в BotFather действительно прописан. Проверка того же рода,
      что имя приложения в boot-логе для Crypto Pay, и стоит она один вызов API —
      против «авторизация молча ложится у всех», если токен чужой.
- [x] ~~`BOT_USERNAME` — под кнопку возврата из оплаченного инвойса.~~
      `BOT_USERNAME=NightRiverPoker_bot` (без `@`; код всё равно срезает его —
      `server/config/app.ts`).
      ⚠️ **Грабли, на которые наступили:** сначала в `.env` уехал буквальный плейсхолдер
      из инструкции — `BOT_USERNAME=ИМЯ_БОТА`. Приложение стартует как ни в чём не бывало,
      а `getMiniAppUrl()` собирает `https://t.me/ИМЯ_БОТА`, и это всплыло бы только на
      кнопке «вернуться в приложение» в оплаченном инвойсе. Инвойсов ещё не было.
      **Вывод: проверять контейнер, а не файл** — `docker compose exec app printenv BOT_USERNAME`.
- [ ] ⚠️ **Нажать Start у нового бота со своего аккаунта.** Telegram не разрешает боту
      писать первым: пока владелец не откроет с ним диалог, суточный дамп и алерты
      `backup.sh` будут молча падать с `403 bot can't initiate conversation` — при
      совершенно верном `BACKUP_TG_CHAT_ID`. На стенде этого не видно, там
      @Testpoke_bot запущен давно, поэтому грабли и не всплывали.
      **Статус на 2026-08-12 неизвестен** — закроется первым дошедшим суточным дампом.

## Фаза C. `.env` боевой машины

**ЗАКРЫТА 2026-08-12.** Все ключи таблицы стоят на машине. Проверялись **внутри
контейнера** (`docker compose exec app printenv`), а не в файле: compose пробрасывает
окружение явным списком, и «в `.env` правильно» само по себе ничего не доказывает.

| Переменная | Значение |
|---|---|
| `CRYPTO_PAY_TOKEN` | боевой токен |
| `CRYPTO_PAY_TESTNET` | `false` |
| `BOT_TOKEN` | токен боевого бота |
| `BOT_USERNAME` | боевой бот |
| `MONEY_INVARIANT_BASELINE_CHIPS` | `0` |
| `SENTRY_DSN` | DSN проекта |
| `SENTRY_ENVIRONMENT` | `production` |
| `POSTGRES_PASSWORD`, `ADMIN_PASS`, `JWT_SECRET` | сгенерировать по отдельности |
| `BACKUP_REMOTE` | `b2:tgp-backups/prod` (см. A3) |
| `BACKUP_AGE_RECIPIENT`, `BACKUP_TG_CHAT_ID` | как на стенде |

Остальные пороги (`WITHDRAWAL_MIN_CHIPS` 1000, `WITHDRAWAL_MAX_CHIPS` 2 500 000,
`WITHDRAWAL_DAILY_LIMIT_CHIPS` 50 000) — по умолчанию либо задать явно.

**`WITHDRAWAL_MIN_HANDS` не задаём** (дефолт 0) — решено 2026-08-05 по итогам разбора,
как порог устроен у крупных румов. Причины, по которым он снят:
- **Он фармится бесплатно.** `countHandsSince` считает строки `HandHistory`, а строка
  пишется на каждое занятое место (`Game.ts` `perPlayer` фильтрует только `p !== null`),
  и сит-аут игрока из `seats` не убирает. Сесть на Funnel 1/2, нажать sit out, подождать
  полчаса — порог пройден без единой фишки риска.
- **Он гейтит не то плечо транзита.** Схема «A вносит → сливает B → B выводит» упирается
  в аккаунт B, у которого `lastDeposit === null`, а это в `withdrawals.ts` явный пропуск
  (`requiredHands: 0`). Проверяется тот, кто не выводит.
- **Чурн уже оплачен.** Комиссию Crypto Pay (3%) платит игрок, кредитуется нетто
  (`depositReconciliation.ts` §D, решение 2026-07-21), так что круг «завёл-вывел» стоит
  ему денег и не стоит нам ничего.
- **Своё плечо закрыто по построению**: `transfer` уходит на тот же Telegram-аккаунт.

Что держит транзит вместо порога: ручное одобрение каждой выплаты + флаги
`NO_HANDS_SINCE_DEPOSIT` / `POSSIBLE_TRANSIT`, которые `computeWithdrawalFlags` выдаёт
независимо от порога — случай «внёс и сразу выводит, не играв» админ видит подсвеченным.
Механизм из кода не выкинут: env-переменная включит его за минуту, если живая аудитория
покажет злоупотребление.

⚠️ **Любой новый ключ в `.env` нужно продублировать в `environment:` файла
`docker-compose.prod.yml`** — compose пробрасывает переменные явным списком. На эти грабли
проект наступал трижды (`CRYPTO_PAY_TOKEN`, `WITHDRAWAL_*`, `SENTRY_DSN`). Все ключи из
таблицы выше на 2026-08-04 в списке есть.

## Фаза D. Запуск и проверка boot-лога — ✅ ВЫПОЛНЕНА 2026-08-12

- [x] ~~`cd /opt/tg-poker && bash update.sh`~~ **2026-08-12** приложение перезапущено
      с полным набором боевых ключей; все три контейнера healthy.
      💡 Если менялся только `.env`, а код нет — полный `update.sh` не нужен и лишний:
      он тянет git и пересобирает образ. Достаточно
      `docker compose -f docker-compose.prod.yml up -d app` — compose пересоздаёт
      контейнер сам, увидев изменившееся окружение (так 2026-08-12 подхватился
      `CRYPTO_PAY_TOKEN`).
- [x] В логе ожидается:
      ```
      [Boot] Sentry initialized
      [Boot] Crypto Pay connected as app "<боевое имя>"
      [Boot] Crypto Pay fee: N.NN% (...)
      [Boot] deposit reconciliation started
      [Boot] money invariant check started
      ```
      **Имя приложения в третьей строке — главный признак, что взят нужный токен.**
      Если там «Buckram Wisent App» — на машине testnet-токен, остановиться немедленно.
      **Все пять строк уже наблюдались 2026-08-12**, ещё с заглушкой `BOT_TOKEN`:
      имя `Night River App`, комиссия `3.00% (built-in default)`. После вписывания
      боевого `BOT_TOKEN` лог читается заново — это единственное место, где видно,
      что плейсхолдеров не осталось.
- [ ] ⚠️ Правки в самом `update.sh` применяются только со **следующего** прогона: bash читает
      скрипт по мере выполнения, а `git pull` подменяет файл. Если деплой должен подхватить
      изменённый `update.sh` — запустить дважды.

## Фаза E. Боевой smoke на минимальных суммах

Повтор того, что отработано на testnet 2026-07-25 — быстро, но пропускать нельзя:
платёжное приложение новое, и ограничения у него свои.

**ПРОЙДЕН 2026-08-12.** Суммы взяли крупнее плана — депозит $20 вместо $5, потому
что вывод $10 надо было чем-то обеспечить: депозит идёт **в** кошелёк приложения,
а выплата **из** него, и $5 покрыли бы только половину.

- [x] ~~**Депозит $5** из мини-аппа.~~ **Депозит $20, инвойс 59771648.** Комиссия
      $0.60 (3.00%), зачислено нетто **1940 фишек**. В леджере `deposit/completed`,
      `meta: {"fee":"0.6","asset":"USDT","paidAmount":"20","requestedChips":2000}`.
      В логе: `[Deposit] credited invoice 59771648: +1940 chips to 158394554 (webhook)`.
      **Метка `(webhook)` закрывает открытый пункт фазы B**: деньги пришли пушем от
      CryptoBot, а не были подобраны реконсиляцией — иначе это неразличимо.
      Наблюдённые 3.00% совпали со встроенным дефолтом, то есть нетто на экране
      депозита считалось верно с первой операции.
- [x] ~~**Кнопка возврата** в CryptoBot после оплаты ведёт в приложение.~~ Ведёт.
- [x] ~~**Вывод $10** (минимум): заявка → админка → Approve.~~ `transferId 3153233`,
      `transferStatus: completed`, USDT дошли. **Кошелёк приложения: 19.40 → 9.40 USDT,
      ровно $10.00** — см. «Открытые вопросы»: комиссии у `transfer` на mainnet нет.
      Фишки списываются в момент подачи заявки, а не одобрения (`balance_after: 940`
      при `status: pending`), так что окна «поставить за столом и одновременно вывести»
      не существует.
- [x] ~~**Инвариант**: `✓ balanced`, drift 0.~~ Сходится до цента: леджер
      1940 − 1000 = 940 = баланс игрока 940 = кошелёк $9.40.
      ⚠️ Как читать логи: `startMoneyInvariantCheck` дёргает колбэк **только при
      расхождении** (оттуда — `Sentry.captureMessage`), поэтому отсутствие строк
      `[Invariant]` означает «дрейфа нет», а не «проверка не работает».
- [x] ~~**Флаги вместо порога**~~ — бейдж `NO_HANDS_SINCE_DEPOSIT` в очереди был.
      `POSSIBLE_TRANSIT` **не сработал, и это верно**: он требует вывода ≥ 90%
      депозита (`WITHDRAWAL_FLAG_RATIO_PCT`), а вывели 1000 из 1940 = 51.5%.
      Флаги считаются на чтении очереди (`server/payments/withdrawals.ts:135`),
      в `meta` транзакции их нет — искать там бесполезно.
- [x] ~~**Бэкап боевой базы** уже отработал хотя бы раз.~~ Почасовые идут с
      2026-08-11 17:17; дамп 17:17 уже содержит обе транзакции смоука (11 718 →
      11 880 байт), лежит в `b2:tgp-backups/prod/hourly/`.
      ⚠️ **Суточное плечо (оно же heartbeat) ещё не проверено.** 2026-08-12 в 03:17 UTC
      оно отработало с заглушкой вместо токена: копия в `daily/` легла, а
      `sendDocument` вернул `404 Not Found` → `FAILED at step 'telegram sendDocument'`.
      **Алерт о поломке идёт через тот же `BOT_TOKEN`, поэтому доставить его было
      нечем** — молчание в личке неотличимо от «всё хорошо». Первая настоящая
      проверка — 03:17 UTC 2026-08-13, и она же проверит, открыт ли диалог с ботом.

## Найдено на смоуке — ✅ ЗАКРЫТО 2026-08-12

Оба исправлены в ветке `fix/payment-copy`, обкатаны на стенде владельцем и выкачены
в прод (`3f36055`). Проверка деплоя шла не по факту сборки, а по содержимому бандла,
который отдаётся браузеру: строка `CryptoBot balance` в нём есть, `Telegram wallet`
отсутствует.

- [x] ~~**Экран вывода отправляет игрока не в то приложение.**~~ Обе строки
      (`Withdraw.tsx:238` и `:254`) теперь говорят «your CryptoBot balance» —
      согласованно с экраном депозита и юридическими текстами.
- [x] ~~**Инвойс CryptoBot обещает больше фишек, чем игрок получит.**~~ Текст счёта
      вынесен в общий `describeDepositInvoice()` (`server/payments/depositFee.ts`) и
      зовётся из обоих мест — депозита игрока и пополнения банкролла. Вид:
      `Deposit $20.00 → 1,940 chips (after 3% Crypto Pay fee)`.
      **Починен не текст, а причина:** каждое место собирало фразу само, поэтому
      расхождение и стало возможным; теперь арифметика живёт в одном месте с той,
      что показывает экран депозита. Ставка берётся наблюдённая, так что смена
      комиссии провайдером уедет в оба места сама.
      Вопрос «писать ли ≈» снялся сам: экран и счёт считают по одной и той же ставке
      и совпадают по построению, а названная в тексте комиссия объясняет число, если
      фактическое зачисление всё же разойдётся. Регрессионные тесты — в
      `server/__tests__/depositFee.test.ts` (11 штук, включая «net, а не gross»).

<details>
<summary>Исходные формулировки находок (как были записаны до фикса)</summary>

- **Экран вывода отправляет игрока не в то приложение.** Текст обещает
      «your Telegram wallet», а `transfer` зачисляет на баланс в **@CryptoBot**.
      Telegram Wallet (@wallet) — отдельный продукт, и игрок пойдёт искать деньги
      туда, где их нет; в худшем случае решит, что выплата не дошла, и напишет
      в поддержку. Две строки:
      - `client/src/pages/Withdraw.tsx:238` — «The USDT arrives in your Telegram
        wallet once approved.»
      - `client/src/pages/Withdraw.tsx:254` — «Paid out — $N sent to your Telegram
        wallet.»
      Остальное приложение формулирует верно: экран депозита говорит «Complete the
      payment in CryptoBot», ToS и Privacy — «Crypto Pay (@CryptoBot)». То есть
      расходится именно экран вывода, и правка сводится к согласованию с ними.

- **Инвойс CryptoBot обещает больше фишек, чем игрок получит.** В описании счёта
      стоит запрошенная сумма, а зачисляется нетто: на депозите 2026-08-12 чек говорил
      «Deposit 2000 chips», а на баланс легли 1940 (комиссия $0.60, 3%). Экран депозита
      в самом мини-аппе комиссию и нетто показывает верно — расходится только текст
      счёта, то есть последнее, что игрок видит перед оплатой.
      Два места, оба берут **gross** `amountChips`:
      - `server/index.ts:851` — `description: \`Deposit ${amountChips} chips\`` (игроки);
      - `server/admin/adminMutations.ts:355` — `Bankroll deposit ${amountChips} chips`
        (пополнение банкролла ботов; те же цифры, но деньги свои).
      ⚠️ Точное нетто на момент создания счёта **неизвестно**: ставка комиссии
      наблюдается из истории оплаченных инвойсов (`server/payments/depositFee.ts`) и
      может смениться на стороне провайдера между созданием и оплатой. Значит писать
      надо оценку, а не точное число — например `$20.00 → ≈1940 chips (3% CryptoBot fee)`,
      либо вовсе убрать фишки из описания и оставить сумму в USDT, чтобы единственным
      местом с арифметикой остался экран мини-аппа.

</details>

## Фаза F. Перевод нынешнего сервера в статус стенда

С 2026-08-12 у стенда появилась вторая, постоянная роль: **обязательная ступень
деплоя**. Любое изменение доезжает до боевой машины только так — ветка → `main` →
стенд → ручная проверка владельцем → прод (`CLAUDE.md`, `DEPLOY.md`). То есть
стенд теперь не «машина, которую не жалко», а часть процесса выпуска, и его
работоспособность — условие любого деплоя на прод.

- [x] ~~`SENTRY_ENVIRONMENT=staging` в его `.env`.~~ **Сделано 2026-08-11**, заранее
      (см. фазу 0): без этого стенд рапортовал `production` и правило алертов было
      не к чему привязать.
- [x] ~~`BACKUP_REMOTE=b2:tgp-backups/staging`~~ — **сделано 2026-08-12**, вместе со
      старыми файлами: `rclone move` перенёс корневые `hourly/` (168 файлов, 16.3 МБ)
      и `daily/` (11 файлов) в `staging/`, боевые 28 файлов в `prod/` не тронуты.
      Пробный `bash backup.sh` лёг в `b2:tgp-backups/staging/hourly/`, ретеншен
      отработал внутри нового префикса. **В корне бакета теперь ровно два имени —
      `prod/` и `staging/`.**
      Почему перенос, а не просто переключение: ретеншен чистит только внутри своего
      префикса, и оставленные в корне файлы стали бы вечными — их не удалил бы никто.
      Заодно снят тихий риск: следующая машина с дефолтным `BACKUP_REMOTE` попадала
      бы в namespace стенда и начала бы затирать его дампы.
- [x] ~~Домен и URL тестового бота — по решению из A1.~~ Менять нечего: стенд как
      стоял на `tgp.isgood.host`, так и остаётся, тестовый бот смотрит туда же.
- [x] ~~Оставить testnet-токен Crypto Pay и тестового бота как есть.~~ Сверено
      2026-08-12: `CRYPTO_PAY_TESTNET=true`. Боевой токен на стенд не попадал.
- [x] ~~Пароли на стенде.~~ **Решено владельцем 2026-08-12: оставляем как есть.**
      `ADMIN_PASS` и `POSTGRES_PASSWORD` там совпадают (сверено в тот же день) и
      разводиться пока не будут. План изначально и не требовал этого («не обязательно»),
      так что фаза F закрыта полностью.
      Что остаётся верным и без ротации: на стенде лежат настоящие Telegram-аккаунты
      живых людей, поэтому утечка его БД — это утечка персональных данных, даже когда
      деньги testnet. Пересмотреть стоит, если стенд начнёт собирать заметно больше
      аккаунтов или его админка окажется доступна кому-то ещё. Дешёвая половина фикса
      на тот случай: сменить **только** `ADMIN_PASS` — он читается из `.env` и базы не
      касается, тогда как ротация `POSTGRES_PASSWORD` требует ещё и `ALTER USER`.
      На боевой машине все три секрета и так различаются — там вопроса нет.

**Прочее в `.env` стенда, что осмотрено и менять не надо:**
`MONEY_INVARIANT_BASELINE_CHIPS=1164` — доледжерный остаток именно этой базы, на
боевой он ноль; `NODE_ENV=production` — так и задумано (на нём висят CORS, запрет
дев-авторизации и `assertSafeBootOrExit`), стенд от прода отличает
`SENTRY_ENVIRONMENT`, а не это.

## Фаза G. После запуска

- [ ] Первую неделю раз в день смотреть карточку инварианта и `[Invariant]` в логах.
- [ ] Следить за очередью Payouts: заявки подтверждаются вручную, флаги (`POSSIBLE_TRANSIT`,
      `NO_HANDS_SINCE_DEPOSIT`, `NEVER_DEPOSITED`) — повод присмотреться, а не автоблокировка.
- [ ] Пополнить bot bankroll реальным депозитом, если нужна массовка за столами.
- [ ] Проверить, что суточный бэкап боевой базы приходит в личку (он же heartbeat).
      Первая настоящая проверка — 03:17 UTC 2026-08-13: до 2026-08-12 там стоял
      плейсхолдер вместо `BOT_TOKEN`, и `sendDocument` отвечал `404`.
- [ ] **`manifest.webmanifest` отдаётся как `application/octet-stream`.** В
      `mime.types` nginx нет расширения `.webmanifest`, отсюда и дефолтный тип.
      **Решено 2026-08-12 не чинить отдельным деплоем — подцепить к следующему
      изменению**, которое и так пойдёт через ветку и стенд: правка лежит в `nginx/`,
      а значит требует пересоздания контейнера nginx, и ради одной строки трогать
      только что сошедшиеся прод и стенд не стоит.
      Строка: `types { application/manifest+json webmanifest; }`.
      Почему не срочно (проверено на проде 2026-08-12): манифест нужен только для
      установки сайта как приложения — имя, иконки, `standalone`, цвет заставки, —
      а мини-апп живёт в WebView Telegram, который ничего не устанавливает.
      `apple-touch-icon` и `theme-color` заданы отдельными тегами и от манифеста не
      зависят. Заголовка `X-Content-Type-Options: nosniff` в ответе **нет**, поэтому
      браузер вправе разобрать содержимое как JSON — с `nosniff` шанс жёсткого отказа
      был бы заметно выше. Единственный задетый сценарий: установка сайта как PWA из
      обычного браузера мимо Telegram, и там поведение деградирует (имя из `<title>`,
      иконка из favicon), а не ломается.

---

## Откат

Боевая машина отдельная, поэтому откат теперь дешёвый: если `transfer` в боевом приложении
не включат или что-то пойдёт не так, боевой сервер просто останавливается
(`docker compose -f docker-compose.prod.yml stop app`), а стенд продолжает работать
нетронутым. Отката для уже отправленных боевых переводов не существует — поэтому smoke
делается на минимальных суммах.

## Приложение: SQL чистки экономики

**НЕ НУЖЕН при выбранном варианте.** В A4 решено стартовать с пустой базы, так что чистить
нечего. Оставлен только на случай, если решение о переносе аккаунтов будет пересмотрено —
и тогда всё описанное ниже снова обязательно. Выполнять **при остановленном `app`**: `TableManager` держит стеки
сидящих игроков в памяти, они переживут чистку БД и вернутся на баланс при выходе — минтинг.

Файл кладётся на сервер и запускается через `-f`, а не `-c`: вложенные кавычки
PowerShell → ssh → psql не выживают.

```bash
scp wipe-for-mainnet.sql root@<боевой хост>:/opt/tg-poker/
ssh root@<боевой хост> "cd /opt/tg-poker && \
  docker compose -f docker-compose.prod.yml cp wipe-for-mainnet.sql postgres:/tmp/w.sql && \
  docker compose -f docker-compose.prod.yml exec -T postgres psql -U poker -d poker_db -f /tmp/w.sql"
```

```sql
-- wipe-for-mainnet.sql — обнуление перенесённой testnet-экономики.
-- Аккаунты, псевдонимы и аватары СОХРАНЯЮТСЯ: удаляются только деньги и их история.
-- ⚠️ Колонки сессии и аватара — camelCase БЕЗ @map, писать в кавычках:
--    "currentChips", "currentTableId", "currentSeat", "avatarId".
--    balance / telegram_id / display_name / is_bot — обычные snake_case.

\echo '=== ДО: балансы и леджер ==='
SELECT telegram_id, display_name, balance, "currentChips" FROM users
WHERE balance <> 0 OR "currentChips" IS NOT NULL ORDER BY telegram_id;
SELECT type, status, count(1) AS rows, sum(amount) AS chips
FROM transactions GROUP BY type, status ORDER BY type;

BEGIN;

-- 1. Деньги: балансы всех аккаунтов (игроки, боты, house 0, bankroll -1000000)
--    и удержанные сессионные стеки.
UPDATE users
SET balance = 0,
    "currentChips" = NULL,
    "currentTableId" = NULL,
    "currentSeat" = NULL;

-- 2. Леджер целиком: testnet-инвойсы в боевом API не существуют, реконсиляция
--    по их external_id всё равно ничего не найдёт, а инвариант должен стартовать
--    с нуля (baseline = 0).
DELETE FROM transactions;

-- 3. Игровая статистика профилей — косметика, чтобы боевые профили начинались с чистого листа.
UPDATE users
SET hands_played = 0, hands_won = 0, total_winnings = 0, biggest_pot = 0;

-- 4. История раздач: тестовый шум в экране истории игрока. Пункт опциональный —
--    на выводы не влияет (порог по рукам снят, см. фазу C).
DELETE FROM "HandHistory";

COMMIT;

\echo '=== ПОСЛЕ: всё должно быть пусто/нулевое ==='
SELECT count(1) AS nonzero_balances FROM users WHERE balance <> 0;
SELECT count(1) AS held_sessions FROM users WHERE "currentChips" IS NOT NULL;
SELECT count(1) AS ledger_rows FROM transactions;
SELECT count(1) AS users_kept FROM users;   -- аккаунты остаются на месте
```

**Что НЕ трогаем:** `"AdminAuditLog"` — журнал действий администратора, его ценность
в полноте; таблицу `users` не удаляем, чтобы игроки сохранили псевдонимы, аватары
и принятый ToS.

## Открытые вопросы

- ~~Домен (A1) и перенос аккаунтов (A4)~~ — **решено 2026-08-05**: домен остаётся стенду,
  боевая машина получает новый; аккаунты не переносим. Породило одну задачу — параметризовать
  домен (`CORS_ORIGIN` + шаблон nginx), см. A1.
- ~~Комиссия `transfer` на mainnet~~ — **решено 2026-08-12 живым переводом: её нет.**
  На выплате $10 кошелёк приложения ушёл с 19.40 на 9.40 USDT, то есть ровно на сумму
  перевода. Игрок получает запрошенное до цента, показывать комиссию вывода не нужно.
  Комиссию платит только депозитное плечо (3%, платит игрок).
- ~~Порог `WITHDRAWAL_MIN_HANDS=50`~~ — **решено 2026-08-05**: порог снят (дефолт 0),
  обоснование в фазе C. Взамен нужен детектор по контрагенту — какая доля выигрыша
  аккаунта пришла от одного и того же игрока. Это единственное, что реально ловит
  чип-дампинг; данные уже есть (`netDelta` в каждой строке `HandHistory`), считается
  парной агрегацией. Не блокер запуска — задача на первое время после него.
