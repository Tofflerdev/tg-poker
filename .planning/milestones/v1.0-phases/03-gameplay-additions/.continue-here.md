# Continue Here — Phase 03 (gameplay-additions)

**Paused:** 2026-04-20T04:57:11.987Z
**Status:** ready_to_execute (plans drafted & verified, execution not started)
**Progress:** 0 of 6 plans executed

---

## Current State

- **Phase:** 03 — gameplay-additions
- **Location:** `.planning/phases/03-gameplay-additions/`
- **Task position:** plan 0 / 6, task 0 / 0 (nothing executed yet)
- **Last action:** Verified all 6 Phase 3 plans; captured findings in HANDOFF.json. Context hit 95% → paused.

## Completed Work

- `03-CONTEXT.md` — Phase 3 context captured
- `03-RESEARCH.md` — research (motion/react, async queue, checkpoint, privacy, retention)
- `03-UI-SPEC.md` — UI design contract
- `03-VALIDATION.md` — validation strategy
- `03-00-PLAN.md` — Vitest test infra (Wave 0)
- `03-01-PLAN.md` — actionBubble server broadcast (Wave 1, deps: 00)
- `03-02-PLAN.md` — hand-history pipeline + checkpoint + retention (Wave 1, deps: 00)
- `03-03-PLAN.md` — client bubble renderer (Wave 2, deps: 00, 01)
- `03-04-PLAN.md` — hand-history reader server (Wave 2, deps: 00, 02)
- `03-05-PLAN.md` — profile history UI client (Wave 3, deps: 00, 04)

## Remaining Work

Execute plans in dependency order:
1. `execute-03-00` — test infra (wave 0)
2. `execute-03-01` — actionBubble server (wave 1)
3. `execute-03-02` — hand-history pipeline (wave 1) — can run parallel with 01
4. `execute-03-03` — client bubble renderer (wave 2)
5. `execute-03-04` — hand-history reader (wave 2) — can run parallel with 03
6. `execute-03-05` — profile history UI (wave 3)

Recommended: `/clear` → `/gsd-execute-phase 3`.

## Decisions Made

- **Plans verified execution-ready** — self-consistent dependency graph, integration points confirmed at `server/index.ts:139` and `:144`, ~70 unit tests planned, per-plan threat models, grep-verifiable invariants.
- **STATE.md is stale** — says "ready for planning" but all 6 plans are committed. Will self-correct on next STATE.md write during execution.

## Blockers

1. **Uncommitted changes on main** (technical, blocking 03-01/02):
   - `client/src/App.tsx`, `client/src/hooks/useTelegram.ts`, `client/vite.config.ts`
   - `server/index.ts` ← **conflict risk**: plan 03-01 edits lines 139-142, plan 03-02 edits lines 144-146 and boot block 150-153
   - `server/middleware/auth.ts`
   - `.planning/phases/02-design-system-rollout-avatars/02-VERIFICATION.md` (untracked)
   - **Action before execute:** `git diff server/index.ts` and decide commit / stash / integrate.
2. **20 WebP avatar binaries missing** (Phase 2 carryover, non-blocking for Phase 3):
   - `client/src/assets/avatars/` — runtime 404s until shipped.

## Required Reading on Resume

- `.planning/HANDOFF.json` — structured handoff (single source of truth)
- `.planning/phases/03-gameplay-additions/03-00-PLAN.md` — start here (wave 0)
- `.planning/phases/03-gameplay-additions/03-01-PLAN.md` — wave 1 server
- `.planning/phases/03-gameplay-additions/03-02-PLAN.md` — wave 1 pipeline
- `server/index.ts` lines 130-160 — integration points (verify no-ops still present post diff resolution)

## Critical Anti-Patterns to Avoid

- **ActionBubbleEvent MUST NOT include holeCards** (T-3-SCHEMA in 03-01).
- **HandHistory server read MUST strip opponent holeCards unless `showedDown`** — `r.showedDown ? r.holeCards : []` (03-04).
- **Client MUST apply same predicate defensively** — `o.showedDown && o.holeCards.length > 0` (03-05).
- **getHandHistory handler is zero-arg** — read telegramId from `socket.data` only; never trust client-supplied id (T-3-AUTHZ).
- **No hex literals, no `dangerouslySetInnerHTML`** across all client plans — use Neon Strip CSS vars.
- **Splice-before-write in HandHistoryQueue** — `const batch = buffer.splice(0, buffer.length);` drains atomically before async DB call.
- **checkpointSeat writes ONLY `currentChips`/`currentTableId`/`currentSeat`** — no mid-hand ephemeral state (D-17 in 03-02).
- **Do NOT run `db push` to drop columns** — Phase 2 HandHistory model is already migrated.

## Infrastructure State

- Vitest: not yet installed (03-00 installs it).
- motion/react: not yet installed (03-03 adds `motion@^12.38.0`).
- Prisma `HandHistory` model: migrated in Phase 2 with `@@index([telegramId, playedAt(sort: Desc)])`.
- `server/index.ts` no-ops at `:139` and `:144` confirmed intact (as of pause time).

## Minor Non-Blocking Concerns Flagged

- `03-03` motion/react shared transition prop — implementer may refactor.
- `03-05` test #9 useHandHistory unmount test is brittle — acceptable.

## Next Action

```
# 1. Inspect and resolve uncommitted server/index.ts changes
git diff server/index.ts

# 2. Clear context and execute
/clear
/gsd-resume-work   # or directly: /gsd-execute-phase 3
```

## Context Notes

Verified all 6 Phase 3 plans in this session. Key findings:
1. Plans 00-05 are rigorous with grep-enforced invariants, per-plan threat models, explicit DO-NOT-MODIFY guardrails between plans.
2. Dependency graph: 00 → {01, 02} → {03, 04} → 05.
3. Privacy contract consistent: server strips opponent holeCards at read (03-04); client defense-in-depth at render (03-05).
4. ~70 unit tests total.
5. Integration points verified: `server/index.ts:139` and `:144` no-ops exist exactly as 03-01/02 describe.
6. User asked to verify plans; verdict was execution-ready.
7. Context hit 95% — paused before `/gsd-execute-phase 3`.
