---
phase: 01-foundations-design-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - .env.example
autonomous: false
requirements: [RESILIENCE-01]
must_haves:
  truths:
    - "Running `prisma migrate status` reports migration v1_mvp_launch as applied"
    - "User table has all 10 additive columns (avatarId, currentTableId, currentSeat, currentChips, sessionToken, disconnectedAt, lastSeenAt, bannedAt, tosAcceptedAt, tosVersion)"
    - "HandHistory and AdminAuditLog tables exist with the exact schemas locked in D-15/D-16"
    - "Targeted indexes from D-17 exist on HandHistory, AdminAuditLog, User"
    - "`npx prisma generate` succeeds and `npx tsc` still typechecks"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated schema with User additions, HandHistory, AdminAuditLog"
      contains: "model HandHistory"
    - path: "prisma/migrations/*_v1_mvp_launch/migration.sql"
      provides: "Applied migration SQL named v1_mvp_launch"
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/*_v1_mvp_launch/migration.sql"
      via: "prisma migrate dev"
      pattern: "v1_mvp_launch"
---

<objective>
Produce a single Prisma migration named `v1_mvp_launch` that (1) adds 10 additive
nullable columns to `User`, (2) creates the `HandHistory` per-player-row table, (3)
creates the `AdminAuditLog` table with typed core + JSON before/after, and (4) lays
down the targeted indexes from D-17. This is the first real migration in the repo
(current state is `db push`-seeded) so a dev-DB reset is part of the plan.

Purpose: RESILIENCE-01 — all schema required by Phase 3 (HandHistory writers), Phase 4
(session/crash-safety columns), and Phase 5 (AdminAuditLog, bannedAt, ToS columns)
must exist now so downstream phases only write code, never schema.
Output: updated `prisma/schema.prisma`, a committed `prisma/migrations/<ts>_v1_mvp_launch/`
directory, an updated `.env.example` (adds `ALLOW_DEV_AUTH` line — used by Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@prisma/schema.prisma
@server/db/prisma.ts
@server/db/UserRepository.ts
@.env.example
@CLAUDE.md

<interfaces>
<!-- SCHEMAS ARE LOCKED by D-14, D-15, D-16, D-17. Do not modify field names/types. -->

User additive columns (D-14 — all nullable, no defaults beyond what's shown):
  avatarId         String?
  currentTableId   String?
  currentSeat      Int?
  currentChips     Int?
  sessionToken     String?
  disconnectedAt   DateTime?
  lastSeenAt       DateTime?
  bannedAt         DateTime?
  tosAcceptedAt    DateTime?
  tosVersion       String?

HandHistory (D-15 — per-player row, one row per (hand, player)):
  id          String   @id @default(cuid())
  handId      String
  telegramId  String       // stringified BigInt — see Pitfall 5 in RESEARCH
  tableId     String
  playedAt    DateTime @default(now())
  board       String[]
  holeCards   String[]
  seat        Int
  netDelta    Int
  finalChips  Int
  showedDown  Boolean
  won         Boolean
  @@index([telegramId, playedAt(sort: Desc)])
  @@index([playedAt])

AdminAuditLog (D-16 — typed core + JSON before/after):
  id              String   @id @default(cuid())
  adminTelegramId String
  action          String
  targetType      String
  targetId        String
  beforeJson      Json?
  afterJson       Json?
  createdAt       DateTime @default(now())
  @@index([adminTelegramId, createdAt(sort: Desc)])
  @@index([action, createdAt(sort: Desc)])

User-side extra index (D-17):
  @@index([currentTableId])  // for Phase 4 boot recovery
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit prisma/schema.prisma with all v1_mvp_launch changes</name>
  <files>prisma/schema.prisma</files>
  <action>
    Edit `prisma/schema.prisma` in a single pass:

    1. Add the 10 additive columns to the existing `User` model exactly as defined in
       &lt;interfaces&gt; (D-14). Place them grouped after existing stats columns with a
       `// --- v1_mvp_launch additions ---` section header comment above the block.
       Add `@@index([currentTableId])` to the `User` model's attribute block.

    2. Append a new `model HandHistory { ... }` block with fields, types, defaults,
       and both `@@index` entries exactly as defined in &lt;interfaces&gt; (D-15).

    3. Append a new `model AdminAuditLog { ... }` block exactly as defined in
       &lt;interfaces&gt; (D-16), including both `@@index` entries.

    Do NOT add relations between User/HandHistory/AdminAuditLog — per D-15/D-16 these
    are lookup-by-telegramId-string tables, not FK-linked. Do NOT change existing User
    columns. Do NOT add defaults to the new columns (all remain nullable per D-14).

    Verify the schema parses: `npx prisma validate`.
  </action>
  <verify>
    <automated>npx prisma validate &amp;&amp; node -e "const s=require('fs').readFileSync('prisma/schema.prisma','utf8');for(const f of ['avatarId','currentTableId','currentSeat','currentChips','sessionToken','disconnectedAt','lastSeenAt','bannedAt','tosAcceptedAt','tosVersion']){if(!s.includes(f)){console.error('MISSING User field:',f);process.exit(1);}}for(const m of ['model HandHistory','model AdminAuditLog']){if(!s.includes(m)){console.error('MISSING:',m);process.exit(1);}}console.log('schema OK');"</automated>
  </verify>
  <done>
    `prisma validate` passes; grep confirms all 10 User fields and both new models exist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — approve dev DB reset strategy</name>
  <what-built>Schema edits are staged. The repo has no `prisma/migrations/` directory (DB was seeded via `db push`), so `prisma migrate dev --name v1_mvp_launch` will detect drift and prompt to reset the dev DB. Per RESEARCH Pitfall 1, this must be an intentional, approved step — not a surprise.</what-built>
  <how-to-verify>
    Per RESEARCH Option A (recommended, pre-deploy, no prod DB): approve dev DB reset.
    Per RESEARCH Option B (preserve local test data): baseline existing schema first
    via `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma > prisma/migrations/0_init/migration.sql`
    then `prisma migrate resolve --applied 0_init` before running `migrate dev`.

    Answer two questions:
      1. Do you have local test data in the `poker_db` Docker Postgres that you need preserved?
         - NO  → Option A (reset). Type "approved: reset".
         - YES → Option B (baseline). Type "approved: baseline".
      2. Is it OK that `npx prisma migrate dev --name v1_mvp_launch` will be run in the
         next task and (Option A) drop+recreate all local tables, or (Option B) attempt
         to baseline without data loss?
  </how-to-verify>
  <resume-signal>Type "approved: reset" or "approved: baseline" (or describe concerns).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run migrate dev, regenerate client, verify</name>
  <files>prisma/migrations/**, .env.example</files>
  <action>
    Based on the checkpoint decision:

    **If "approved: reset" (Option A):**
      1. Ensure Docker Postgres is up: `docker-compose up -d` (if not already).
      2. Run `npx prisma migrate dev --name v1_mvp_launch` and accept the reset prompt.
         This generates `prisma/migrations/<timestamp>_v1_mvp_launch/migration.sql`,
         drops+recreates the dev DB, and applies the migration.
      3. Run `npx prisma generate` (normally auto-run, but ensure client is fresh).

    **If "approved: baseline" (Option B):**
      1. `mkdir -p prisma/migrations/0_init`
      2. Checkout the pre-edit schema temporarily (`git stash` the current edits), run
         `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma > prisma/migrations/0_init/migration.sql`,
         then `git stash pop` to restore edits.
      3. `npx prisma migrate resolve --applied 0_init`
      4. `npx prisma migrate dev --name v1_mvp_launch` — generates the additive-diff
         migration only.
      5. `npx prisma generate`.

    **Both paths, finalize:**
      - Append to `.env.example` a commented line:
        ```
        # Enables the dev-only auth bypass. MUST be unset or false in production.
        # ALLOW_DEV_AUTH=false
        ```
        (Used by Plan 05; listing here since we're already touching .env.example for
        schema-related env changes, and Plan 05 depends on this variable being
        documented.)
      - Run `npx tsc --noEmit -p tsconfig.json` to confirm the server still typechecks
        against the regenerated Prisma client (the new User columns are additive so
        existing code compiles; Prisma client includes them as optional).
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&amp;1 | tee /tmp/mig.log; grep -q "v1_mvp_launch" /tmp/mig.log || (echo "migration not found in status" &amp;&amp; exit 1); ls prisma/migrations/*v1_mvp_launch/migration.sql &amp;&amp; npx prisma validate &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; grep -q "ALLOW_DEV_AUTH" .env.example</automated>
  </verify>
  <done>
    `prisma migrate status` shows `v1_mvp_launch` as applied; migration SQL file committed
    under `prisma/migrations/<ts>_v1_mvp_launch/`; Prisma client regenerated; server
    typechecks; `.env.example` documents `ALLOW_DEV_AUTH`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ↔ PostgreSQL | Prisma client is the only path; no raw SQL exposed. Migration SQL is applied server-side by the Prisma CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T (Tampering) | Migration file checked into git | mitigate | Migration SQL committed with checksum; Prisma detects drift on next migrate. Code review gate before merge. |
| T-01-02-02 | I (Information Disclosure) | `HandHistory.holeCards` column could leak opponents' cards via profile views | mitigate | Schema lands now, but the write path (Phase 3) must filter holeCards to the owning telegramId only at read time. Documented in D-15 and PROFILE-04. |
| T-01-02-03 | I (Information Disclosure) | `AdminAuditLog.beforeJson/afterJson` may capture PII in admin mutations | mitigate | Phase 5 (ADMIN-06) writers MUST scrub raw initData / sessionToken before persistence. Schema-only phase; flagged for Phase 5 implementers. |
| T-01-02-04 | D (DoS) | Unbounded `HandHistory` growth | mitigate | D-17 index `HandHistory(playedAt)` supports the Phase 3 retention job that drops rows &gt; 90 days (PROFILE-04). Index lands now. |
| T-01-02-05 | R (Repudiation) | Admin actions must be auditable | mitigate | `AdminAuditLog` schema lands now; Phase 5 ADMIN-06 enforces "audit write BEFORE mutation commit". |
| T-01-02-06 | S (Spoofing) | `adminTelegramId` column is a plain String, not FK | accept | D-16 locks this. Admin identification happens at namespace middleware (Phase 5 ADMIN-02); audit log records the verified id at call time. No FK needed. |
</threat_model>

<verification>
- `npx prisma migrate status` → `v1_mvp_launch` applied.
- Migration SQL file exists under `prisma/migrations/<ts>_v1_mvp_launch/`.
- `npx prisma validate` passes.
- `npx tsc --noEmit` passes (server).
- `.env.example` contains `ALLOW_DEV_AUTH` documentation line.
</verification>

<success_criteria>
- RESILIENCE-01 closed: all v1.0 schema needs land in one atomic migration.
- Phase 3 (HandHistory writers) and Phase 5 (AdminAuditLog writers) can rely on the
  tables/columns existing without any further schema work.
- Phase 4 boot recovery can query `User.currentTableId` via an index.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-02-SUMMARY.md`
</output>
