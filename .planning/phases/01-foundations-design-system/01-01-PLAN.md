---
phase: 01-foundations-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/styles/neon.css
  - client/src/App.tsx
  - client/src/components/GameControls.tsx
  - client/src/components/SeatsDisplay.tsx
autonomous: true
requirements: [BRAND-03]
must_haves:
  truths:
    - "Neon Strip palette is defined once in client/src/styles/neon.css and nowhere else"
    - "GameControls.tsx and SeatsDisplay.tsx contain no hard-coded neon hex literals"
    - "Client production build (vite build) succeeds and emits no missing CSS var warnings"
  artifacts:
    - path: "client/src/styles/neon.css"
      provides: "Single source of truth for Neon Strip tokens via @theme"
      contains: "@theme"
    - path: "client/src/components/GameControls.tsx"
      provides: "Consumes tokens via var(--color-...) / Tailwind classes"
    - path: "client/src/components/SeatsDisplay.tsx"
      provides: "Consumes tokens via var(--color-...) / Tailwind classes"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/styles/neon.css"
      via: "import side-effect"
      pattern: "import .*neon\\.css"
    - from: "client/src/components/GameControls.tsx"
      to: "client/src/styles/neon.css"
      via: "CSS custom property references"
      pattern: "var\\(--color-action-|var\\(--glow-"
---

<objective>
Promote the Neon Strip palette out of component-local `NEON` literal objects into a single
Tailwind v4 `@theme` block at `client/src/styles/neon.css`, and refactor the two existing
consumers (`GameControls.tsx`, `SeatsDisplay.tsx`) to read tokens via CSS custom properties
(`var(--color-action-fold)` etc.). Proves the token system end-to-end before the Phase 2
redesign rollout.

Purpose: BRAND-03 — one palette definition. Phase 2 consumers (new `ui/` primitives) will
import the same vars; no drift.
Output: `neon.css` + two refactored component files containing zero hard-coded neon hex
values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@client/src/styles/telegram.css
@client/src/components/GameControls.tsx
@client/src/components/SeatsDisplay.tsx
@client/src/App.tsx
@client/vite.config.ts
@CLAUDE.md

<interfaces>
<!-- Token names are LOCKED by D-02. Use these exact names. -->

Semantic action-tier colors (D-02):
  --color-action-fold   #ff4757   (red)
  --color-action-call   #00e5ff   (cyan — also used for check)
  --color-action-raise  #ffab00   (amber)
  --color-action-allin  #ff6d00   (orange)
  --color-action-sit    #4caf50   (green)
  --color-active        #00e5ff   (cyan — active turn glow)
  --color-chip          #ffab00   (amber — stack numerals)
  --color-neutral       #b0bec5   (gray)

Derived rgba glow tokens (captured from existing NEON.glow values):
  --glow-fold    rgba(255, 71, 87, 0.35)
  --glow-call    rgba(0, 229, 255, 0.30)
  --glow-raise   rgba(255, 171, 0, 0.35)
  --glow-allin   rgba(255, 109, 0, 0.40)
  --glow-sit     rgba(76, 175, 80, 0.35)
  --glow-neutral rgba(176, 190, 197, 0.15)

Tailwind v4: tokens declared inside `@theme { ... }` become both CSS custom
properties AND auto-generated utilities (bg-action-fold, text-action-fold, etc.).
Consumers may use either `var(--color-action-fold)` in inline style or the
generated utility class — both resolve to the same token.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create neon.css with @theme block</name>
  <files>client/src/styles/neon.css, client/src/App.tsx</files>
  <action>
    Create `client/src/styles/neon.css` with the token set defined in &lt;interfaces&gt; above
    (per D-01 / D-02). File structure:
      1. `@import "tailwindcss";` as first line (matches `telegram.css` convention).
      2. `@theme { ... }` block declaring all 8 `--color-*` tokens and 6 `--glow-*` tokens.
      3. Include a leading comment: `/* Neon Strip — single source of truth. Do not add hex literals to components; consume these tokens. See .planning/phases/01-foundations-design-system/01-CONTEXT.md D-01/D-02. */`

    Wire import into `client/src/App.tsx`: add `import "./styles/neon.css";` next to the
    existing `import "./styles/telegram.css";` line (ordering: telegram.css first, then
    neon.css — later import wins on collisions, which is intended for token overrides).

    Do NOT delete or alter `telegram.css`. Do NOT create a parallel JS token object
    (per "Don't Hand-Roll" table in RESEARCH). Consumers read CSS vars directly.
  </action>
  <verify>
    <automated>cd client && npm run build 2>&amp;1 | tee /tmp/neon-build.log; grep -q "error" /tmp/neon-build.log &amp;&amp; exit 1; grep -c "\-\-color-action-fold" src/styles/neon.css</automated>
  </verify>
  <done>
    `neon.css` exists with all 8 color tokens + 6 glow tokens inside one `@theme` block;
    `App.tsx` imports it; `npm run build` in `client/` succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor GameControls.tsx and SeatsDisplay.tsx to consume tokens</name>
  <files>client/src/components/GameControls.tsx, client/src/components/SeatsDisplay.tsx</files>
  <action>
    Remove the `NEON` literal objects at the top of both files. Replace every consumer
    reference with the CSS custom property:
      - `NEON.fold.color`  → `'var(--color-action-fold)'`
      - `NEON.call.color`  → `'var(--color-action-call)'`
      - `NEON.raise.color` → `'var(--color-action-raise)'`
      - `NEON.allin.color` → `'var(--color-action-allin)'`
      - `NEON.sit.color`   → `'var(--color-action-sit)'`
      - `NEON.neutral.color` → `'var(--color-neutral)'`
      - Active/glow ring around avatar / pulsing seat border → `'var(--color-active)'`
      - Stack numeral colors → `'var(--color-chip)'`
      - Glow shadow rgba (e.g. inside `box-shadow`, `text-shadow`) → `'var(--glow-fold)'`
        / `--glow-call` / `--glow-raise` / `--glow-allin` / `--glow-sit` / `--glow-neutral`

    Keep all existing markup, animations, and structural classes untouched — only the
    color/glow literal strings change. Do NOT introduce new utility classes or rewrite
    the component architecture (Phase 2 does the redesign).

    Per D-03 acceptance: after this task, grepping for the known hex literals
    (`ff4757`, `00e5ff`, `ffab00`, `ff6d00`, `4caf50`, `b0bec5`) in these two files
    must return zero matches (case-insensitive).

    Also delete the `NEON = { ... }` object declarations entirely once all call sites
    have been migrated.
  </action>
  <verify>
    <automated>cd client &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; node -e "const fs=require('fs');const files=['client/src/components/GameControls.tsx','client/src/components/SeatsDisplay.tsx'];const hex=/#(ff4757|00e5ff|ffab00|ff6d00|4caf50|b0bec5)/i;for(const f of files){const c=fs.readFileSync(f,'utf8');const m=c.match(hex);if(m){console.error('HEX LITERAL FOUND in',f,':',m[0]);process.exit(1);}if(/const\s+NEON\s*=/.test(c)){console.error('NEON literal object still present in',f);process.exit(1);}}console.log('OK: no hex literals, no NEON objects');"</automated>
  </verify>
  <done>
    Both files build cleanly, contain no neon hex literals, and no longer define a
    local `NEON` object. Visual output is indistinguishable from before (tokens resolve
    to identical values).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none relevant | This plan is CSS-only / design-token refactor; no data, auth, or transport surface touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I (Information Disclosure) | neon.css served to all clients | accept | Tokens are brand constants, not secrets. No PII or credentials in the file. |
| T-01-01-02 | T (Tampering) | Client-side CSS can be overridden by user stylesheets | accept | Visual-only surface; server enforces all authority. No security decision depends on Neon Strip colors. |
</threat_model>

<verification>
- `npm run build` in `client/` succeeds.
- `grep -i "#ff4757\|#00e5ff\|#ffab00\|#ff6d00\|#4caf50\|#b0bec5" client/src/components/GameControls.tsx client/src/components/SeatsDisplay.tsx` returns zero matches.
- Manual (smoke): app still renders with identical palette.
</verification>

<success_criteria>
- BRAND-03 closed: one file defines the palette; the two consumer components reference
  only CSS vars / generated utilities.
- No runtime regressions (same colors render).
- Phase 2 can import `var(--color-*)` from anywhere in `client/src/` without new plumbing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-01-SUMMARY.md`
</output>
