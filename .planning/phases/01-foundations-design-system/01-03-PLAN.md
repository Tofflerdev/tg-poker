---
phase: 01-foundations-design-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - types/index.ts
  - server/Game.ts
  - server/models/Table.ts
  - server/index.ts
autonomous: true
requirements: [GAME-04]
must_haves:
  truths:
    - "Game.ts exposes setOnPlayerAction and setOnHandComplete as public methods"
    - "Every direct action method (fold/check/call/raise/allIn) fires onPlayerAction after its state mutation, before nextPlayer"
    - "onHandComplete fires exactly once per hand — from both the win-by-fold branch in nextStage and the end of showdown"
    - "handId is generated via crypto.randomUUID at startNextHand and held on the Game instance"
    - "server/index.ts wires both callbacks on every Table instance with no-op consumers"
    - "Existing gameplay behavior is unchanged (setOnShowdown/StateChange/TurnTimeout still fire; no extra broadcasts)"
  artifacts:
    - path: "types/index.ts"
      provides: "PlayerActionEvent and HandCompleteEvent payload types"
      exports: ["PlayerActionEvent", "HandCompleteEvent"]
    - path: "server/Game.ts"
      provides: "Callback setters + emission sites + handId generation"
      contains: "setOnPlayerAction"
    - path: "server/models/Table.ts"
      provides: "Per-Game wiring of callbacks"
    - path: "server/index.ts"
      provides: "Registration of no-op callbacks on every Table"
  key_links:
    - from: "server/Game.ts"
      to: "onPlayerAction callback"
      via: "synchronous invocation in fold/check/call/raise/allIn"
      pattern: "this\\.onPlayerAction\\s*\\?"
    - from: "server/Game.ts"
      to: "onHandComplete callback"
      via: "synchronous invocation in nextStage win-by-fold and showdown"
      pattern: "this\\.onHandComplete\\s*\\?"
    - from: "server/index.ts"
      to: "Table.setOnPlayerAction / setOnHandComplete"
      via: "wiring block after table creation"
      pattern: "setOnPlayerAction|setOnHandComplete"
---

<objective>
Add two callback seams to the poker engine — `setOnPlayerAction` and `setOnHandComplete` —
following the established `setOnShowdown` / `setOnStateChange` / `setOnTurnTimeout`
pattern. Wire synchronous fire-and-forget emission (D-09) from the exact sites
identified in RESEARCH. In Phase 1 the consumers in `server/index.ts` are **no-ops** —
wiring exists, behavior does not change. Phase 3 replaces the no-ops with bubble
broadcasts and `HandHistory` queue writes.

Purpose: GAME-04 — downstream phases (3 bubbles, 3 history, 3 chip checkpointing, 4 resilience)
all depend on these two event streams. Defining them now with the final payload shape
(D-10, D-11) means no signature changes later.
Output: 2 new public setters on `Game.ts`, 6 emission call sites, `handId` field + cuid
generation at hand start, 2 new exported types, wiring on every `Table` in `server/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@server/Game.ts
@server/models/Table.ts
@server/index.ts
@types/index.ts
@CLAUDE.md

<interfaces>
<!-- PAYLOAD SHAPES ARE LOCKED by D-10 and D-11. -->

// types/index.ts — new exports

export type PlayerActionKind = 'fold' | 'check' | 'call' | 'bet' | 'raise' | 'allin';

export interface PlayerActionEvent {
  tableId: string;
  telegramId: string;           // stringified; see Pitfall 5
  seat: number;
  action: PlayerActionKind;
  amount: number;               // chips committed by this action (delta)
  totalBetThisStreet: number;   // player.bet after the action
  potAfter: number;             // game.getTotalPot() after the action
}

export interface HandCompletePerPlayer {
  telegramId: string;
  seat: number;
  holeCards: string[];          // server filters before broadcasting — do not broadcast raw
  finalChips: number;
  netDelta: number;             // finalChips - handStartChips[seat]
  won: boolean;
  showedDown: boolean;
}

export interface HandCompleteEvent {
  handId: string;
  tableId: string;
  completedAt: Date;
  board: string[];              // game.communityCards snapshot
  perPlayer: HandCompletePerPlayer[];
}

// Game.ts — setter contract (mirrors existing setOnShowdown pattern at lines 824-834)

public setOnPlayerAction(cb: (evt: PlayerActionEvent) => void): void;
public setOnHandComplete(cb: (evt: HandCompleteEvent) => void): void;

// Game.ts — new private state

private onPlayerAction: ((evt: PlayerActionEvent) => void) | null = null;
private onHandComplete: ((evt: HandCompleteEvent) => void) | null = null;
private currentHandId: string | null = null;
private handStartChips: Record<number, number> = {};  // seat -> chips at startNextHand
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define callback payload types in types/index.ts</name>
  <files>types/index.ts</files>
  <action>
    Append the four exported types from &lt;interfaces&gt; (`PlayerActionKind`,
    `PlayerActionEvent`, `HandCompletePerPlayer`, `HandCompleteEvent`) to
    `types/index.ts`. Place them after existing event types, with a section comment
    `// --- Phase 1 Game callback contracts (GAME-04, D-10/D-11) ---`.

    Do NOT modify any existing type. Do NOT add these to the socket event map — these
    are server-internal callback types, not wire events. Phase 3 will define the
    client-facing `actionBubble` wire event; that mapping is out of scope here.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const s=require('fs').readFileSync('types/index.ts','utf8');for(const t of ['PlayerActionEvent','HandCompleteEvent','PlayerActionKind','HandCompletePerPlayer']){if(!new RegExp('export (interface|type) '+t).test(s)){console.error('MISSING type:',t);process.exit(1);}}console.log('types OK');"</automated>
  </verify>
  <done>
    Four types exported from `types/index.ts`; `tsc` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add setters, handId, and emission sites to Game.ts</name>
  <files>server/Game.ts</files>
  <behavior>
    - `setOnPlayerAction(cb)` stores cb; second call overwrites (matches existing setter pattern).
    - `setOnHandComplete(cb)` stores cb; second call overwrites.
    - When `fold()/check()/call()/raise()/allIn()` succeed, `onPlayerAction` fires exactly once with the correct action kind, `amount` = chips committed by THIS action (not cumulative), `totalBetThisStreet` = player.bet post-mutation, `potAfter` = getTotalPot() post-mutation.
    - When `fold()` is invoked from `removePlayer` auto-fold path, `onPlayerAction` STILL fires (per RESEARCH Pitfall 6 — emit from the fold() method itself, single source).
    - `startNextHand` assigns `this.currentHandId = crypto.randomUUID()` and snapshots per-seat starting chips into `this.handStartChips`.
    - `onHandComplete` fires exactly ONCE per hand: once from the win-by-fold branch in `nextStage()` (right before `this.onShowdown(...)`), once from the end of `showdown()` (right before `return this.lastShowdown`). `runOutBoard` reaches showdown() so it's covered transitively — do NOT add a third call site.
    - After emitting `onHandComplete`, clear `this.currentHandId = null` to guard against double-emit.
    - All callback invocations are `this.onPlayerAction?.(evt)` — null-checked, sync, return value ignored.
  </behavior>
  <action>
    Edit `server/Game.ts`:

    1. **Imports** (top of file): add `import crypto from 'crypto';` if not already present.
       Add `import type { PlayerActionEvent, HandCompleteEvent, PlayerActionKind } from '../types/index.js';`

    2. **Private fields** (near existing `onShowdown` / `onStateChange` / `onTurnTimeout`
       private fields — around lines 820-ish): add the four fields from &lt;interfaces&gt;
       (`onPlayerAction`, `onHandComplete`, `currentHandId`, `handStartChips`).

    3. **Public setters** (next to `setOnTurnTimeout/StateChange/Showdown` at lines
       824-834): add `setOnPlayerAction` and `setOnHandComplete` following the exact
       shape of `setOnShowdown`.

    4. **`startNextHand`**: at the top of the method (after pre-condition guards but
       before deck deal), add:
       ```ts
       this.currentHandId = crypto.randomUUID();
       this.handStartChips = {};
       for (const p of this.seats) if (p) this.handStartChips[p.seat] = p.chips;
       ```

    5. **Emission in action methods** — for each of `fold`, `check`, `call`, `raise`,
       `allIn` (lines will vary; consult RESEARCH §"Emission sites for `onPlayerAction`"):
       insert just BEFORE the trailing `this.nextPlayer();` in each method:
       ```ts
       this.onPlayerAction?.({
         tableId: this.tableId,
         telegramId: player.id,   // after refactor, player.id === telegramId (stringified); if Plan 04 hasn't landed yet on your working branch, use String(player.telegramId ?? player.id)
         seat: player.seat,
         action: '<fold|check|call|bet|raise|allin>' as PlayerActionKind,
         amount: <delta_chips_committed_by_this_call>,
         totalBetThisStreet: player.bet,
         potAfter: this.getTotalPot(),
       });
       ```
       Action kind mapping:
         - `fold()`   → `'fold'`,   amount: `0`
         - `check()`  → `'check'`,  amount: `0`
         - `call()`   → `'call'`,   amount: the `actualBet` local variable (chips actually paid)
         - `raise()`  → `'raise'`,  amount: the delta added this action (computed locally — total raise minus what the player had already put in this street)
         - `allIn()`  → `'allin'`,  amount: the delta added this action (chips moved from stack to pot this call)
       If `Game.ts` has a `tableId` field — use it. If not, add `private tableId: string` to the constructor signature; callers in `Table.ts` already know the tableId and can pass it.

    6. **Emission at hand completion** — two sites only:
       a) `nextStage()` win-by-fold branch (RESEARCH lines 473-502): right before
          `this.onShowdown(this.lastShowdown!)`, build and fire the `HandCompleteEvent`
          using `this.currentHandId`, `this.tableId`, `new Date()`, `this.communityCards`,
          and `perPlayer` derived from `this.seats.filter(Boolean)` with
          `netDelta = p.chips - (this.handStartChips[p.seat] ?? p.chips)`.
       b) End of `showdown()` (RESEARCH lines 651-672): right before
          `return this.lastShowdown;`, fire the same shape of event.
       After each emission, set `this.currentHandId = null`.

    7. **Do NOT** add a third emission in `runOutBoard()` — it calls into `showdown()`
       (path 2 covers it) and a third call would double-fire.

    Keep formatting/indentation consistent with surrounding code. Do not rename existing
    methods or alter their signatures.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const s=require('fs').readFileSync('server/Game.ts','utf8');for(const p of ['setOnPlayerAction','setOnHandComplete','currentHandId','handStartChips','randomUUID']){if(!s.includes(p)){console.error('MISSING:',p);process.exit(1);}}const playerActionCalls=(s.match(/this\.onPlayerAction\?\.\(/g)||[]).length;const handCompleteCalls=(s.match(/this\.onHandComplete\?\.\(/g)||[]).length;if(playerActionCalls!==5){console.error('Expected 5 onPlayerAction emit sites, found',playerActionCalls);process.exit(1);}if(handCompleteCalls!==2){console.error('Expected 2 onHandComplete emit sites, found',handCompleteCalls);process.exit(1);}console.log('Game.ts emit sites OK: 5 action, 2 hand-complete');"</automated>
  </verify>
  <done>
    `tsc` passes; grep confirms exactly 5 `onPlayerAction?.(` call sites and exactly 2
    `onHandComplete?.(` call sites; `currentHandId` generated via `crypto.randomUUID`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire callbacks in Table.ts and register no-op consumers in index.ts</name>
  <files>server/models/Table.ts, server/index.ts</files>
  <action>
    **`server/models/Table.ts`**: expose two pass-through methods that forward to the
    underlying `Game` instance:
      ```ts
      setOnPlayerAction(cb: (evt: PlayerActionEvent) => void) { this.game.setOnPlayerAction(cb); }
      setOnHandComplete(cb: (evt: HandCompleteEvent) => void) { this.game.setOnHandComplete(cb); }
      ```
    Add the necessary type import (`import type { PlayerActionEvent, HandCompleteEvent } from '../../types/index.js';`). Place next to any existing pass-through setters
    on the Table class (setOnShowdown etc. if present; otherwise at the end of the class).

    If `Game.ts` constructor now takes `tableId`, pass `this.id` (or whatever Table's id
    field is named) when constructing the Game instance inside Table.

    **`server/index.ts`**: find the single site where `Table` instances are created or
    first retrieved at server startup (typically inside `TableManager` init, iterated
    via `tableManager.listTables()` or similar). After tables exist, add a registration
    block — ideally in a helper `wireGameCallbacks(table: Table)` that's called once per
    table (either inline in `TableManager` boot, or in `server/index.ts` after
    `tableManager` is ready):

      ```ts
      for (const table of tableManager.getAllTables()) {
        table.setOnPlayerAction((evt) => {
          // Phase 1: no-op. Phase 3 broadcasts actionBubble. Phase 3 writes HandHistory.
          // Keep block empty to preserve Phase 1 success criterion "no behavior change".
        });
        table.setOnHandComplete((evt) => {
          // Phase 1: no-op. Phase 3 queues HandHistory writes; Phase 3 checkpoints chips.
        });
      }
      ```

    If `TableManager` has no `getAllTables()` method, add one that returns
    `Array.from(this.tables.values())`.

    Place this wiring AFTER all tables are constructed and BEFORE `server.listen()`.

    Do NOT add any side effect inside the callbacks. Phase 1's success criterion
    explicitly requires "no behavior change to existing gameplay".
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; node -e "const ti=require('fs').readFileSync('server/models/Table.ts','utf8');const si=require('fs').readFileSync('server/index.ts','utf8');if(!/setOnPlayerAction\s*\(/.test(ti)||!/setOnHandComplete\s*\(/.test(ti)){console.error('Table.ts missing pass-through setters');process.exit(1);}if(!/setOnPlayerAction\s*\(/.test(si)||!/setOnHandComplete\s*\(/.test(si)){console.error('index.ts missing callback registration');process.exit(1);}console.log('wiring OK');"</automated>
  </verify>
  <done>
    `Table.setOnPlayerAction` / `setOnHandComplete` pass-through methods exist; every
    Table in `tableManager` has both callbacks registered with no-op bodies in
    `server/index.ts`; `tsc` passes; `npm run dev` still starts and plays a hand
    without regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Game engine → server handlers | Callbacks fire inside the game loop synchronously. Listeners MUST NOT block on I/O (D-09). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | D (DoS) | Synchronous listener blocks the game loop and freezes all tables | mitigate | D-09 locks fire-and-forget semantics. Phase 1 consumers are no-op (can't block). Phase 3 implementers MUST queue to an async worker (see PROFILE-02) and return immediately. Documented in the no-op comments. |
| T-01-03-02 | I (Information Disclosure) | `HandCompleteEvent.perPlayer.holeCards` includes every player's hole cards | mitigate | Server-internal event — NEVER broadcast raw. Phase 3 bubble broadcast filters to metadata only; Phase 3 HandHistory writer persists each row under its owning telegramId. Documented in D-11 comment and type JSDoc. |
| T-01-03-03 | T (Tampering) | Second `setOnPlayerAction` call overwrites the first | accept | Matches existing setter pattern (setOnShowdown etc.). Only `server/index.ts` calls these; no third-party registration surface. |
| T-01-03-04 | R (Repudiation) | Missing `onHandComplete` emission = missing HandHistory row in Phase 3 | mitigate | Dual emission sites are covered (win-by-fold and showdown). `runOutBoard` transitively covered via showdown. `currentHandId = null` guard prevents double-fire. Grep check in verify step enforces exactly 2 emission sites. |
| T-01-03-05 | S (Spoofing) | `telegramId` field in event payload could be empty if Plan 04 not yet landed | mitigate | During Plan 04's refactor, `player.id` becomes the telegramId. In the interim, the code falls back to `String(player.telegramId ?? player.id)`. Plan 04 simplifies this to `player.id`. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after all three tasks.
- Grep confirms exactly 5 `this.onPlayerAction?.(` sites and exactly 2 `this.onHandComplete?.(` sites in `Game.ts`.
- `crypto.randomUUID` is invoked inside `startNextHand`.
- `server/index.ts` registers both callbacks on every Table (grep finds both setter calls).
- Manual smoke: `npm run dev` starts, a hand plays to completion, no new broadcasts appear to clients (Phase 1 no-ops).
</verification>

<success_criteria>
- GAME-04 closed: Game.ts emits the full callback stream described in D-10/D-11 with
  no behavior change to existing gameplay (Phase 1 consumers are no-ops).
- Phase 3 can attach real handlers (bubble broadcast, HandHistory queue, chip
  checkpointing) without changing Game.ts signatures.
- `handId` is stable within a hand and distinct across hands.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-03-SUMMARY.md`
</output>
