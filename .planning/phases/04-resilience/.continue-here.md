---
context: phase
phase: 04-resilience
workflow: gsd-plan-phase
workflow_step: 8 (spawn gsd-planner) — pending
status: in_progress
last_updated: 2026-04-29T05:39:48.019Z
---

<current_state>
Mid `/gsd-plan-phase 4`. Research and validation are done; the planner has not been spawned yet. CONTEXT.md was already in place; the user selected "Research first (Recommended)" when asked. The researcher returned `## RESEARCH COMPLETE` with HIGH confidence (commit `52db3d7`), and 04-VALIDATION.md was just written from the research's Validation Architecture section (uncommitted).

Next concrete action: commit 04-VALIDATION.md, then spawn gsd-planner per plan-phase.md step 8.
</current_state>

<completed_work>

- 04-CONTEXT.md — pre-existing, decisions A/B/C/D locked (commits `8596cff` / `6be0ccf`)
- 04-RESEARCH.md — generated by gsd-phase-researcher with `## Validation Architecture` section; commit `52db3d7`
- 04-VALIDATION.md — drafted from template + research; 5 Wave-0 test files mapped to RESILIENCE-* requirements; UNCOMMITTED
</completed_work>

<remaining_work>

1. Commit 04-VALIDATION.md (`docs(04): add validation strategy`)
2. Spawn `gsd-planner` (plan-phase.md §8) with the planner prompt below
3. Spawn `gsd-plan-checker` (§10), iterate revision loop max 3 (§12)
4. Requirements coverage gate (§13) — verify RESILIENCE-02/04/05/06/07 each appear in a plan's `requirements` field
5. Record planning completion: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" state planned-phase --phase 04 --name resilience --plans <count>`
6. Present final status; route to `/gsd-execute-phase 4`
</remaining_work>

<decisions_made>

- Research-first selected over skip-research — Nyquist gate is enabled and the new server primitives (SessionRecovery, GraceRegistry, atomic balance helpers) warranted RESEARCH.md.
- Plans should key off the four CONTEXT.md decision blocks (A reconnect handshake, B grace + overlay server, C boot recovery, D atomic balance) plus E client overlay — researcher's recommendation. Likely 5 plans across 2-3 waves with one Wave-0 plan for the test scaffolding.
- 5 Wave-0 test files: `GraceRegistry.test.ts`, `SessionRecovery.test.ts`, `UserRepository.atomic.test.ts`, `reconnectHandshake.test.ts`, `ReconnectOverlay.test.tsx` — paths and patterns documented in 04-VALIDATION.md.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/04-resilience/04-CONTEXT.md` — locked user decisions; do not contradict
2. `.planning/phases/04-resilience/04-RESEARCH.md` — domain investigation, pitfalls, validation architecture, security domain
3. `.planning/phases/04-resilience/04-VALIDATION.md` — Wave-0 test inventory and per-requirement test mapping (planner aligns Wave-0 plan to this)
4. `.planning/ROADMAP.md` §"Phase 4: Resilience" — goal + 5 success criteria + requirement IDs
5. `.planning/REQUIREMENTS.md` — RESILIENCE-02/04/05/06/07 verbatim
6. `.planning/phases/01-foundations-design-system/01-CONTEXT.md` — Phase 1 substrate (telegramId keying, session columns, eviction scaffold)
7. `.planning/phases/03-gameplay-additions/03-CONTEXT.md` — Phase 3 chip checkpoint write path (mid-hand state must NEVER persist)
8. `CLAUDE.md` — Neon Strip UI, Prisma v7, Socket.io transport
9. `server/index.ts` — auth handler `:208`, joinTable `:487`, leaveTable `:547`, disconnect `:798`, boot block `:182`, eviction callback `:230`
10. `server/TableManager.ts` — `setSocketForTelegram` eviction (`:262`), `getPlayerTable`, `socketByTelegram` map
11. `prisma/schema.prisma` — confirms `User.@@index([currentTableId])` exists for boot-recovery scan

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Persisting mid-hand ephemeral state | Hole cards / current bets / turn timer must NEVER hit the DB; engine is I/O-free (Phase 1 contract); boot recovery refunds rather than reseating into a hand | blocking | Plans MUST NOT add columns or writes for in-flight state. Boot-recovery is always-refund (D-C1). Re-read 03-CONTEXT.md §"Chip Checkpoint" before planning. |
| Reseat-as-sit-out on boot | The success-criterion-4 wording mentions a reseat option, but D-C1 explicitly chose always-refund. Do not introduce a reseat branch. | blocking | Plan must implement a single refund path for every persisted session row. |
| Read-then-write transactions for balance | Race-prone (Concern #5). Phase 4 must use Prisma `updateMany` with conditional `WHERE` — single round-trip. | blocking | All buy-in / refund paths use the atomic helpers `tryDecrementBalance` and `refundCurrentChips` in `server/db/UserRepository.ts` (or new `BalanceRepository.ts`). No `$queryRaw`, no read-then-write. |
| Adding `replacedBySession` payload | D-A3 locked: bare event with no payload, then `socket.disconnect(true)`. | advisory | Type as `replacedBySession: () => void` in `ExtendedServerEvents` and remove the `'sessionReplaced' as any` cast at `server/index.ts:239`. |
| New socket event names for reconnect snapshot | D-A2 locked: reuse existing `tableJoined` + `state` events on reconnect. No new `reconnectSnapshot` event type. | advisory | Auth handler at `server/index.ts:208` calls `setSocketForTelegram`, then if `getPlayerTable(tid)` returns a table, emits `tableJoined { tableId, seat, state: getStateForPlayer(tid) }` and runs `updateTableState(tableId)`. |

## Infrastructure State

- PostgreSQL via `docker-compose up -d` — assume running for execution; not needed for planning step.
- Vitest + RTL already configured (Phase 3 plan 03-00) — no framework install in Wave 0; 5 new test files only.
- No new npm dependencies expected.

## Pre-Execution Critique Required

N/A — pausing inside planning, not between design and execution.

<context>
The plan-phase workflow is at step 8 of 14. Researcher returned HIGH confidence; the seven domain pitfalls were catalogued (the most subtle: re-arming the 30s mid-hand timer to 120s when the hand ends mid-grace via the existing `setOnHandComplete` listener at server/index.ts:160). Five Wave-0 test files identified, all under existing Vitest infrastructure.

When resuming: spawn `gsd-planner` (subagent_type=gsd-planner, model=opus) with the prompt template below. Use the existing CONTEXT/RESEARCH/VALIDATION as the planner's source-of-truth.

Planner prompt skeleton (fill in `${...}`):

```markdown
<planning_context>
**Phase:** 04
**Mode:** standard

<files_to_read>
- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/04-resilience/04-CONTEXT.md (USER DECISIONS — locked)
- .planning/phases/04-resilience/04-RESEARCH.md (Technical Research + Validation Architecture)
- .planning/phases/04-resilience/04-VALIDATION.md (Wave-0 test inventory)
- (no UI-SPEC; client overlay design lives in CONTEXT D-B4)
</files_to_read>

**Phase requirement IDs (every ID MUST appear in a plan's `requirements` field):** RESILIENCE-02, RESILIENCE-04, RESILIENCE-05, RESILIENCE-06, RESILIENCE-07

**Project instructions:** Read ./CLAUDE.md
**Project skills:** Check .claude/skills/ — read SKILL.md files if present
</planning_context>

<downstream_consumer>
Output consumed by /gsd-execute-phase. Plans need:
- Frontmatter (wave, depends_on, files_modified, autonomous, requirements)
- Tasks in XML format with read_first and acceptance_criteria fields (MANDATORY on every task)
- Verification criteria
- must_haves for goal-backward verification
</downstream_consumer>

<deep_work_rules>
[verbatim from plan-phase.md §8]
</deep_work_rules>

<quality_gate>
[verbatim from plan-phase.md §8]
</quality_gate>
```

After planner returns `## PLANNING COMPLETE`, spawn `gsd-plan-checker` per §10 with the same files plus all newly-written `*-PLAN.md` files. Iterate up to 3 revision passes. Then run requirements coverage gate, record state, and present final status.
</context>

<next_action>
1. `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(04): add validation strategy" --files .planning/phases/04-resilience/04-VALIDATION.md`
2. Spawn `gsd-planner` (model: opus, description: "Plan Phase 4") with the prompt above.
3. On `## PLANNING COMPLETE`, continue plan-phase.md workflow from §10.
</next_action>
