This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
client/21429.png
client/315254.png
client/index.html
client/package.json
client/src/App.tsx
client/src/assets/cards/2C.png
client/src/assets/cards/2D.png
client/src/assets/cards/2H.png
client/src/assets/cards/2S.png
client/src/assets/cards/3C.png
client/src/assets/cards/3D.png
client/src/assets/cards/3H.png
client/src/assets/cards/3S.png
client/src/assets/cards/4C.png
client/src/assets/cards/4D.png
client/src/assets/cards/4H.png
client/src/assets/cards/4S.png
client/src/assets/cards/5C.png
client/src/assets/cards/5D.png
client/src/assets/cards/5H.png
client/src/assets/cards/5S.png
client/src/assets/cards/6C.png
client/src/assets/cards/6D.png
client/src/assets/cards/6H.png
client/src/assets/cards/6S.png
client/src/assets/cards/7C.png
client/src/assets/cards/7D.png
client/src/assets/cards/7H.png
client/src/assets/cards/7S.png
client/src/assets/cards/8C.png
client/src/assets/cards/8D.png
client/src/assets/cards/8H.png
client/src/assets/cards/8S.png
client/src/assets/cards/9C.png
client/src/assets/cards/9D.png
client/src/assets/cards/9H.png
client/src/assets/cards/9S.png
client/src/assets/cards/AC.png
client/src/assets/cards/AD.png
client/src/assets/cards/AH.png
client/src/assets/cards/AS.png
client/src/assets/cards/back.png
client/src/assets/cards/JC.png
client/src/assets/cards/JD.png
client/src/assets/cards/JH.png
client/src/assets/cards/JS.png
client/src/assets/cards/KC.png
client/src/assets/cards/KD.png
client/src/assets/cards/KH.png
client/src/assets/cards/KS.png
client/src/assets/cards/QC.png
client/src/assets/cards/QD.png
client/src/assets/cards/QH.png
client/src/assets/cards/QS.png
client/src/assets/cards/TC.png
client/src/assets/cards/TD.png
client/src/assets/cards/TH.png
client/src/assets/cards/TS.png
client/src/components/Card.tsx
client/src/components/CommunityCards.tsx
client/src/components/GameControls.tsx
client/src/components/HandDisplay.tsx
client/src/components/SeatsDisplay.tsx
client/src/components/Table.tsx
client/src/index.tsx
client/tsconfig.json
client/vite.config.ts
package.json
repomix-output.txt
server/Deck.ts
server/Game.ts
server/index.ts
server/types.ts
ts-node-esm.js
tsconfig.json
types/pokersolver.d.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="repomix-output.txt">
This file is a merged representation of the entire codebase, combining all repository files into a single document.
Generated by Repomix on: 2025-12-15T19:27:08.217Z

================================================================
File Summary
================================================================

Purpose:
--------
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.

File Format:
------------
The content is organized as follows:
1. This summary section
2. Repository information
3. Repository structure
4. Multiple file entries, each consisting of:
  a. A separator line (================)
  b. The file path (File: path/to/file)
  c. Another separator line
  d. The full contents of the file
  e. A blank line

Usage Guidelines:
-----------------
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.

Notes:
------
- Some files may have been excluded based on .gitignore rules and Repomix's
  configuration.
- Binary files are not included in this packed representation. Please refer to
  the Repository Structure section for a complete list of file paths, including
  binary files.

Additional Info:
----------------

For more information about Repomix, visit: https://github.com/yamadashy/repomix

================================================================
Repository Structure
================================================================
.gitignore
client/index.html
client/package.json
client/src/App.tsx
client/src/components/Card.tsx
client/src/components/CommunityCards.tsx
client/src/components/GameControls.tsx
client/src/components/HandDisplay.tsx
client/src/components/SeatsDisplay.tsx
client/src/components/Table.tsx
client/src/index.tsx
client/tsconfig.json
client/vite.config.ts
package.json
server/Deck.ts
server/Game.ts
server/index.ts
server/types.ts
ts-node-esm.js
tsconfig.json
types/pokersolver.d.ts

================================================================
Repository Files
================================================================

================
File: .gitignore
================
node_modules/
client/node_modules/

================
File: client/index.html
================
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Poker MVP React</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/index.tsx"></script>
  </body>
</html>

================
File: client/package.json
================
{
  "name": "poker-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "typescript": "^5.1.3",
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}

================
File: client/src/App.tsx
================
import React, { useEffect, useState } from "react";
import { io, Socket } from "socket.io-client";
import GameControls from "./components/GameControls";
import Table from "./components/Table";

interface Player {
  id: string;
  seat?: number;
  hand: string[];
}

interface GameState {
  seats: (Player | null)[];
  spectators: Player[];
  communityCards: string[];
}

interface ShowdownResult {
  results: any[];
  winners: any[];
}

const socket: Socket = io("http://localhost:3000");

const App: React.FC = () => {
  const [state, setState] = useState<GameState>({
    seats: Array(6).fill(null),
    spectators: [],
    communityCards: [],
  });
  const [showdown, setShowdown] = useState<ShowdownResult | null>(null);
  const [mySeat, setMySeat] = useState<number | null>(null);

  useEffect(() => {
    socket.on("state", (gameState: GameState) => {
      setState(gameState);
      setShowdown(null);
    });

    socket.on("showdown", (result: ShowdownResult) => setShowdown(result));

    socket.on("errorMessage", (msg: string) => alert(msg));

    // 🚀 сразу при подключении запросим актуальное состояние
    socket.emit("getState");

    return () => {
      socket.off("state");
      socket.off("showdown");
      socket.off("errorMessage");
    };
  }, []);

  const handleSit = (seat: number) => {
    socket.emit("join", seat);
    setMySeat(seat);
  };

  return (
    <div
      style={{
        padding: 20,
        fontFamily: "sans-serif",
        color: "#f1f1f1",
        background: "#2e2e2e",
        minHeight: "100vh",
      }}
    >
      <h1 style={{ textAlign: "center" }}>♠️ Poker MVP React</h1>
      <GameControls socket={socket} />

      <Table
        seats={state.seats}
        spectators={state.spectators}
        mySeat={mySeat}
        communityCards={state.communityCards}
        onSit={handleSit}
      />

      <h2>Showdown</h2>
      <pre>{showdown ? JSON.stringify(showdown, null, 2) : "—"}</pre>
    </div>
  );
};

export default App;

================
File: client/src/components/Card.tsx
================
import React from "react";

interface CardProps {
  code?: string; // например "AS", "10H", "KD" или пустая строка
  size?: number; // размер карты в пикселях
  style?: React.CSSProperties;
}

const Card: React.FC<CardProps> = ({ code, size = 60, style }) => {
  // 🔹 путь к картинкам
  const src = code
    ? `/src/assets/cards/${code}.png`
    : `/src/assets/cards/back.png`; // <-- рубашка

  return (
    <img
      src={src}
      alt={code || "back"}
      style={{
        width: size,
        height: size * 1.4, // стандартное соотношение карт
        borderRadius: 6,
        boxShadow: "0 2px 5px rgba(0,0,0,0.5)",
        transition: "transform 0.3s",
        ...style,
      }}
    />
  );
};

export default Card;

================
File: client/src/components/CommunityCards.tsx
================
import React from "react";
import Card from "./Card"; // используем ваш компонент Card

interface CommunityCardsProps {
  cards: string[];
  size?: number;      // размер карты в пикселях
  spacing?: number;   // расстояние между картами
}

const CommunityCards: React.FC<CommunityCardsProps> = ({
  cards,
  size = 60,
  spacing = 10, // уменьшено до 10px
}) => {
  return (
    <div
      style={{
        display: "flex",
        gap: `${spacing}px`,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      {cards.map((code, idx) => (
        <Card key={idx} code={code} size={size} />
      ))}
    </div>
  );
};

export default CommunityCards;

================
File: client/src/components/GameControls.tsx
================
import React from "react";
import { Socket } from "socket.io-client";

interface Props {
  socket: Socket;
}

const GameControls: React.FC<Props> = ({ socket }) => {
  return (
    <div style={{ marginBottom: "20px" }}>
      <button onClick={() => socket.emit("start")}>Start Game</button>
      <button onClick={() => socket.emit("flop")}>Flop</button>
      <button onClick={() => socket.emit("turn")}>Turn</button>
      <button onClick={() => socket.emit("river")}>River</button>
      <button onClick={() => socket.emit("showdown")}>Showdown</button>
      <button onClick={() => socket.emit("reset")}>Reset Game</button>
    </div>
  );
};

export default GameControls;

================
File: client/src/components/HandDisplay.tsx
================
import React from "react";
import Card from "./Card";

interface HandDisplayProps {
  cards: string[];
  title?: string;
  size?: number;    // размер карты в пикселях
  overlap?: number; // сколько пикселей карты накладываются друг на друга
}

const HandDisplay: React.FC<HandDisplayProps> = ({
  cards,
  title,
  size = 60,
  overlap = 20,
}) => {
  const containerHeight = size * 1.4; // высота подложки
  const totalWidth = cards.length > 0 ? size + (cards.length - 1) * (size - overlap) : 0;

  return (
    <div style={{ marginBottom: 20 }}>
      {title && <h3>{title}</h3>}
      <div
        style={{
          position: "relative",
          height: containerHeight,
          width: totalWidth, // ширина подложки равна ширине всей руки
          margin: "0 auto",  // горизонтальное центрирование подложки
        }}
      >
        {cards.map((c, i) => (
          <Card
            key={i}
            code={c}
            size={size}
            style={{
              position: "absolute",
              left: i * (size - overlap),
              top: 0,
            }}
          />
        ))}
      </div>
    </div>
  );
};

export default HandDisplay;

================
File: client/src/components/SeatsDisplay.tsx
================
import React from "react";
import HandDisplay from "./HandDisplay";

interface Player {
  id: string;
  seat?: number; // у наблюдателей может не быть места
  hand: string[];
}

interface SeatsDisplayProps {
  seats: (Player | null)[];
  mySeat: number | null;
  tableWidth: number;
  tableHeight: number;
  seatSize?: number;
  seatOffset?: number;
  onSit: (seat: number) => void;
}

const SeatsDisplay: React.FC<SeatsDisplayProps> = ({
  seats,
  mySeat,
  tableWidth,
  tableHeight,
  seatSize = 120,
  seatOffset = 50,
  onSit,
}) => {
  return (
    <>
      {seats.map((player, i) => {
        const angle = (i / seats.length) * 2 * Math.PI;
        const radiusX = tableWidth / 2 + seatOffset;
        const radiusY = tableHeight / 2 + seatOffset;
        const x = radiusX * Math.cos(angle);
        const y = radiusY * Math.sin(angle);

        const isFree = !player;
        const canSit = isFree && mySeat === null;

        return (
          <div
            key={i}
            style={{
              position: "absolute",
              left: tableWidth / 2 + x - seatSize / 2,
              top: tableHeight / 2 + y - seatSize / 2,
              width: seatSize,
              height: seatSize,
              borderRadius: 12,
              background: canSit ? "#4a7a4a" : isFree ? "#3a5a3a" : "#444",
              border: `2px solid ${canSit ? "#aaffaa" : isFree ? "#777" : "#555"}`,
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              justifyContent: "center",
              color: "white",
              textAlign: "center",
              cursor: canSit ? "pointer" : "default",
              transition: "background 0.2s, border 0.2s",
            }}
            onMouseEnter={(e) => {
              if (canSit) e.currentTarget.style.background = "#5fbf5f";
            }}
            onMouseLeave={(e) => {
              if (canSit) e.currentTarget.style.background = "#4a7a4a";
            }}
            onClick={() => canSit && onSit(i)}
          >
            {player ? (
              <HandDisplay cards={player.hand} title={`Player`} />
            ) : (
              <>
                <div>Место свободно</div>
                {canSit && <div style={{ marginTop: 5 }}>Сесть</div>}
              </>
            )}
          </div>
        );
      })}
    </>
  );
};

export default SeatsDisplay;

================
File: client/src/components/Table.tsx
================
import React from "react";
import SeatsDisplay from "./SeatsDisplay";
import CommunityCards from "./CommunityCards";

interface Player {
  id: string;
  seat?: number; // у наблюдателей места нет
  hand: string[];
}

interface TableProps {
  seats?: (Player | null)[];      // делаем необязательными
  spectators?: Player[];          // делаем необязательными
  mySeat: number | null;
  tableWidth?: number;
  tableHeight?: number;
  seatSize?: number;
  seatOffset?: number;
  communityCards?: string[];      // делаем необязательными
  onSit: (seat: number) => void;
}

const Table: React.FC<TableProps> = ({
  seats = [],                      // дефолт — пустой массив
  spectators = [],                 // дефолт — пустой массив
  mySeat,
  tableWidth = 700,
  tableHeight = 400,
  seatSize = 120,
  seatOffset = 50,
  communityCards = [],             // дефолт — пустой массив
  onSit,
}) => {
  return (
    <>
      <div
        style={{
          position: "relative",
          width: tableWidth,
          height: tableHeight,
          margin: "60px auto",
          borderRadius: "50% / 30%",
          background: "green",
          border: "10px solid brown",
        }}
      >
        {/* Игроки вокруг стола */}
        <SeatsDisplay
          seats={seats}
          mySeat={mySeat}
          tableWidth={tableWidth}
          tableHeight={tableHeight}
          seatSize={seatSize}
          seatOffset={seatOffset}
          onSit={onSit}
        />

        {/* Community Cards в центре стола */}
        <div
          style={{
            position: "absolute",
            top: "50%",
            left: "50%",
            transform: "translate(-50%, -50%)",
          }}
        >
          <CommunityCards cards={communityCards} spacing={10} size={60} />
        </div>
      </div>

      {/* Наблюдатели под столом */}
      <div style={{ marginTop: 20 }}>
        <h3>Наблюдатели</h3>
        <div style={{ display: "flex", gap: 10, flexWrap: "wrap" }}>
          {spectators.map((s) => (
            <div
              key={s.id}
              style={{
                padding: "5px 10px",
                background: "#555",
                borderRadius: 5,
                color: "#fff",
              }}
            >
              {s.id}
            </div>
          ))}
        </div>
      </div>
    </>
  );
};

export default Table;

================
File: client/src/index.tsx
================
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

================
File: client/tsconfig.json
================
{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}

================
File: client/vite.config.ts
================
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173
  }
});

================
File: package.json
================
{
  "name": "poker-mvp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pokersolver": "^2.1.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "@types/socket.io": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}

================
File: server/Deck.ts
================
// server/Deck.ts
// Колода карт и безопасное перемешивание (Fisher-Yates + crypto.randomInt)

import { randomInt } from "crypto";

export default class Deck {
  private cards: string[] = [];

  constructor() {
    const suits = ["s", "h", "d", "c"]; // пики, червы, бубны, трефы
    const ranks = ["2", "3", "4", "5", "6", "7", "8", "9", "T", "J", "Q", "K", "A"]; // T = Ten
    this.cards = [];
    for (const suit of suits) {
      for (const rank of ranks) {
        this.cards.push(`${rank}${suit}`); // пример: "As", "Td"
      }
    }
    this.shuffle();
  }

  // Криптографически более надёжное тасование
  shuffle() {
    for (let i = this.cards.length - 1; i > 0; i--) {
      const j = randomInt(0, i + 1);
      [this.cards[i], this.cards[j]] = [this.cards[j], this.cards[i]];
    }
  }

  // Раздать n карт — удаляются из колоды
  deal(n: number): string[] {
    return this.cards.splice(0, n);
  }
}

================
File: server/Game.ts
================
import Deck from "./Deck.js";
import pkg from "pokersolver";
const { Hand } = pkg;

export interface Player {
  id: string;
  seat: number;
  hand: string[];
  chips: number;           // стек игрока
  bet: number;             // текущая ставка в раунде
  folded: boolean;         // сбросил карты
  allIn: boolean;          // пошел ва-банк
}

export interface GameState {
  seats: (Player | null)[];
  spectators: { id: string }[];
  communityCards: string[];
  pot: number;
  currentBet: number;
  currentPlayer: number | null;
  dealerPosition: number;
  smallBlind: number;
  bigBlind: number;
  stage: 'waiting' | 'preflop' | 'flop' | 'turn' | 'river' | 'showdown';
}

export default class Game {
  private seats: (Player | null)[] = Array(6).fill(null);
  private spectators: { id: string }[] = [];
  private communityCards: string[] = [];
  private deck: Deck | null = null;
  
  private pot: number = 0;
  private currentBet: number = 0;
  private currentPlayer: number | null = null;
  private dealerPosition: number = 0;
  private smallBlind: number = 10;
  private bigBlind: number = 20;
  private stage: GameState['stage'] = 'waiting';
  private lastRaisePosition: number | null = null;

  // Добавление игрока
  addPlayer(id: string, seat: number, chips: number = 1000): boolean {
    if (seat < 0 || seat >= this.seats.length) return false;
    if (this.seats[seat]) return false;
    if (this.stage !== 'waiting') return false; // нельзя добавлять во время игры

    this.spectators = this.spectators.filter((p) => p.id !== id);

    const player: Player = {
      id,
      seat,
      hand: [],
      chips,
      bet: 0,
      folded: false,
      allIn: false,
    };
    this.seats[seat] = player;
    return true;
  }

  addSpectator(id: string): boolean {
    if (!this.spectators.find((p) => p.id === id)) {
      this.spectators.push({ id });
    }
    return true;
  }

  removePlayer(id: string) {
    this.seats = this.seats.map((p) => (p?.id === id ? null : p));
    this.spectators = this.spectators.filter((p) => p.id !== id);
  }

  reset() {
    this.deck = null;
    this.communityCards = [];
    this.pot = 0;
    this.currentBet = 0;
    this.currentPlayer = null;
    this.stage = 'waiting';
    this.lastRaisePosition = null;
    
    this.seats.forEach((p) => {
      if (p) {
        p.hand = [];
        p.bet = 0;
        p.folded = false;
        p.allIn = false;
      }
    });
  }

  start() {
    const activePlayers = this.getActivePlayers();
    if (activePlayers.length < 2) {
      throw new Error("Нужно минимум 2 игрока");
    }

    this.reset();
    this.deck = new Deck();
    this.deck.shuffle();
    this.stage = 'preflop';

    // Раздача карт
    this.seats.forEach((p) => {
      if (p && p.chips > 0) {
        p.hand = this.deck!.deal(2);
      }
    });

    // Установка блайндов
    this.postBlinds();
    
    // Первый игрок после большого блайнда
    this.currentPlayer = this.getNextPlayer(this.getBigBlindPosition());
  }

  private postBlinds() {
    const sbPos = this.getSmallBlindPosition();
    const bbPos = this.getBigBlindPosition();

    const sbPlayer = this.seats[sbPos];
    const bbPlayer = this.seats[bbPos];

    if (sbPlayer) {
      const sbAmount = Math.min(this.smallBlind, sbPlayer.chips);
      sbPlayer.bet = sbAmount;
      sbPlayer.chips -= sbAmount;
      this.pot += sbAmount;
      if (sbPlayer.chips === 0) sbPlayer.allIn = true;
    }

    if (bbPlayer) {
      const bbAmount = Math.min(this.bigBlind, bbPlayer.chips);
      bbPlayer.bet = bbAmount;
      bbPlayer.chips -= bbAmount;
      this.pot += bbAmount;
      this.currentBet = bbAmount;
      if (bbPlayer.chips === 0) bbPlayer.allIn = true;
    }

    this.lastRaisePosition = bbPos;
  }

  // Действия игрока
  fold(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    player.folded = true;
    this.nextPlayer();
    return true;
  }

  check(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;
    if (player.bet < this.currentBet) return false; // нельзя чекать, нужно коллировать

    this.nextPlayer();
    return true;
  }

  call(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const toCall = this.currentBet - player.bet;
    const actualBet = Math.min(toCall, player.chips);
    
    player.bet += actualBet;
    player.chips -= actualBet;
    this.pot += actualBet;

    if (player.chips === 0) player.allIn = true;

    this.nextPlayer();
    return true;
  }

  raise(playerId: string, amount: number): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const toCall = this.currentBet - player.bet;
    const totalBet = toCall + amount;

    if (totalBet > player.chips) return false; // недостаточно фишек
    if (amount < this.bigBlind) return false; // минимальный рейз = BB

    player.chips -= totalBet;
    player.bet += totalBet;
    this.pot += totalBet;
    this.currentBet = player.bet;

    if (player.chips === 0) player.allIn = true;

    this.lastRaisePosition = player.seat;
    this.nextPlayer();
    return true;
  }

  allIn(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const allInAmount = player.chips;
    player.bet += allInAmount;
    player.chips = 0;
    this.pot += allInAmount;
    player.allIn = true;

    if (player.bet > this.currentBet) {
      this.currentBet = player.bet;
      this.lastRaisePosition = player.seat;
    }

    this.nextPlayer();
    return true;
  }

  // Переход к следующему игроку или следующей стадии
  private nextPlayer() {
    const next = this.getNextPlayer(this.currentPlayer!);
    
    // Проверка: все ли уравняли ставки
    if (this.isBettingRoundComplete()) {
      this.nextStage();
    } else {
      this.currentPlayer = next;
    }
  }

  private isBettingRoundComplete(): boolean {
    const activePlayers = this.getActivePlayers();
    
    // Все игроки (кроме all-in) сделали ход и уравняли ставку
    const playersWhoCanAct = activePlayers.filter(p => !p.allIn);
    
    if (playersWhoCanAct.length === 0) return true;
    
    const allBetsEqual = playersWhoCanAct.every(p => p.bet === this.currentBet);
    const allActed = this.currentPlayer === this.lastRaisePosition || playersWhoCanAct.length === 1;
    
    return allBetsEqual && allActed;
  }

  private nextStage() {
    // Сброс ставок игроков
    this.seats.forEach(p => {
      if (p) p.bet = 0;
    });
    this.currentBet = 0;

    const activePlayers = this.getActivePlayers();
    
    if (activePlayers.length === 1) {
      // Все сбросили, победитель забирает банк
      this.awards([activePlayers[0]]);
      return;
    }

    switch (this.stage) {
      case 'preflop':
        this.flop();
        this.stage = 'flop';
        break;
      case 'flop':
        this.turn();
        this.stage = 'turn';
        break;
      case 'turn':
        this.river();
        this.stage = 'river';
        break;
      case 'river':
        this.stage = 'showdown';
        this.showdown();
        return;
    }

    this.currentPlayer = this.getNextPlayer(this.dealerPosition);
    this.lastRaisePosition = this.currentPlayer;
  }

  flop() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(3));
  }

  turn() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(1));
  }

  river() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(1));
  }

  showdown() {
    const activePlayers = this.getActivePlayers();

    const results = activePlayers.map((p) => {
      const full = [...p.hand, ...this.communityCards];
      const solved = Hand.solve(full);
      return {
        player: p,
        hand: solved,
        descr: solved.descr,
        rank: solved.rank,
      };
    });

    const winnerHands = Hand.winners(results.map(r => r.hand));
    const winners = results.filter(r => winnerHands.includes(r.hand));

    this.awards(winners.map(w => w.player));

    return {
      results: results.map(r => ({
        id: r.player.id,
        seat: r.player.seat,
        hand: r.player.hand,
        descr: r.descr,
        rank: r.rank,
      })),
      winners: winners.map(w => ({
        id: w.player.id,
        descr: w.descr,
      })),
    };
  }

  private awards(winners: Player[]) {
    const share = Math.floor(this.pot / winners.length);
    winners.forEach(w => {
      w.chips += share;
    });
    this.pot = 0;
    this.stage = 'waiting';
    this.currentPlayer = null;
    
    // Передвижение дилера
    this.dealerPosition = this.getNextSeat(this.dealerPosition);
  }

  // Вспомогательные методы
  private getCurrentPlayerIfValid(playerId: string): Player | null {
    if (this.currentPlayer === null) return null;
    const player = this.seats[this.currentPlayer];
    if (!player || player.id !== playerId || player.folded || player.allIn) {
      return null;
    }
    return player;
  }

  private getActivePlayers(): Player[] {
    return this.seats.filter((p): p is Player => 
      p !== null && !p.folded
    );
  }

  private getNextPlayer(fromSeat: number): number {
    let seat = this.getNextSeat(fromSeat);
    let attempts = 0;
    
    while (attempts < this.seats.length) {
      const player = this.seats[seat];
      if (player && !player.folded && !player.allIn && player.chips > 0) {
        return seat;
      }
      seat = this.getNextSeat(seat);
      attempts++;
    }
    
    return fromSeat;
  }

  private getNextSeat(fromSeat: number): number {
    let seat = (fromSeat + 1) % this.seats.length;
    let attempts = 0;
    
    while (attempts < this.seats.length) {
      if (this.seats[seat] !== null) {
        return seat;
      }
      seat = (seat + 1) % this.seats.length;
      attempts++;
    }
    
    return fromSeat;
  }

  private getSmallBlindPosition(): number {
    return this.getNextSeat(this.dealerPosition);
  }

  private getBigBlindPosition(): number {
    return this.getNextSeat(this.getSmallBlindPosition());
  }

  getState(): GameState {
    return {
      seats: this.seats,
      spectators: this.spectators,
      communityCards: this.communityCards,
      pot: this.pot,
      currentBet: this.currentBet,
      currentPlayer: this.currentPlayer,
      dealerPosition: this.dealerPosition,
      smallBlind: this.smallBlind,
      bigBlind: this.bigBlind,
      stage: this.stage,
    };
  }

  // Получить состояние для конкретного игрока (скрыть карты других)
  getStateForPlayer(playerId: string): any {
    const state = this.getState();
    return {
      ...state,
      seats: state.seats.map(p => {
        if (!p) return null;
        if (p.id === playerId || this.stage === 'showdown') {
          return p; // показываем карты
        }
        return { ...p, hand: [] }; // скрываем карты
      }),
    };
  }

  setBlinds(small: number, big: number) {
    if (this.stage !== 'waiting') return false;
    this.smallBlind = small;
    this.bigBlind = big;
    return true;
  }
}

================
File: server/index.ts
================
import express from "express";
import http from "http";
import { Server } from "socket.io";
import Game from "./Game.js";

const app = express();
const server = http.createServer(app);
const io = new Server(server, {
  cors: { origin: "*" },
});

const PORT = 3000;
const game = new Game();

app.get("/", (_req, res) => {
  res.send("Poker server is running");
});

io.on("connection", (socket) => {
  console.log("Player connected:", socket.id);

 const updateState = function () {
    let state = game.getState()
    let s = structuredClone(state)
    let players: string[] = [];
    s.seats.filter((p) => p !== null).map((p) => {
      if (p.id !== null ) {
        players.push(p.id)
      }
    })
    s.spectators.map((p) => {
      if (p.id !== null ) {
        players.push(p.id)
      }
    })

    players.forEach((id) => {
      let playerSeats: any[] = []
      s.seats.map((pl) => {
        let p = structuredClone(pl)
        if (p !== null && p.id !== id && p.hand.length > 0) {
          p.hand = ["back","back"]
        }
        playerSeats.push(p)
      })
      let playerState = {
        seats: playerSeats,
        spectators: s.spectators,
        communityCards: s.communityCards,
      }
      console.log(`Sending state to id# ${id}...`)
      console.log(playerState)
      io.to(id).emit("state", playerState )
    })
    
  }

  // Новый клиент по умолчанию становится наблюдателем
  game.addSpectator(socket.id);

  // Клиент сам может запросить состояние
  socket.on("getState", () => {
    updateState();
  });

  // Игрок хочет занять место
  socket.on("join", (seat: number) => {
    const success = game.addPlayer(socket.id, seat);
    if (!success) {
      socket.emit("errorMessage", "Место занято или неверный номер");
      return;
    }
    updateState();
  });

  socket.on("reset", () => {
    game.reset();
    updateState();
  });

  socket.on("start", () => {
    game.start();
    updateState();
  });

  socket.on("flop", () => {
    game.flop();
    updateState();
  });

  socket.on("turn", () => {
    game.turn();
    updateState();
  });

  socket.on("river", () => {
    game.river();
    updateState();
  });

  socket.on("showdown", () => {
    const result = game.showdown();
    io.emit("state", game.getState())
    io.emit("showdown", result);
  });

  socket.on("disconnect", () => {
    console.log("Player disconnected:", socket.id);
    game.removePlayer(socket.id);
    updateState();
  });
});

server.listen(PORT, () => {
  console.log(`Poker server running on http://localhost:${PORT}`);
});

================
File: server/types.ts
================
export type Player = {
  id: string;
  hand: string[];
};

================
File: ts-node-esm.js
================
#!/usr/bin/env node
import { register } from "ts-node";
import { pathToFileURL } from "node:url";

register({
  transpileOnly: true,
  compilerOptions: {
    module: "NodeNext",
    target: "ES2022"
  }
});

import(pathToFileURL("./server/index.ts").href);

================
File: tsconfig.json
================
{
  "compilerOptions": {
    /* Основные параметры */
    "target": "ES2022",                       /* Современный синтаксис (под Node 18+) */
    "module": "NodeNext",                     /* Поддержка ESM */
    "moduleResolution": "NodeNext",           /* Поиск модулей как в ESM */
    "rootDir": "./server",                    /* Где исходники TypeScript */
    "outDir": "./dist",                       /* Куда собирать JS */

    /* Типизация */
    "strict": true,                           /* Строгий режим */
    "esModuleInterop": true,                  /* Упрощает работу с CommonJS модулями */
    "forceConsistentCasingInFileNames": true,

    /* Поддержка кастомных типов */
    "typeRoots": ["./types", "./node_modules/@types"],

    /* Удобства */
    "skipLibCheck": true                      /* Не проверять типы в node_modules */
  },
  "include": ["server", "types"],             /* Берём исходники и типы */
  "exclude": ["node_modules", "dist"]         /* Игнорируем мусор */
}

================
File: types/pokersolver.d.ts
================
declare module "pokersolver" {
  export type Card = string;

  export class Hand {
    name: string;
    descr: string;
    rank: number;
    cards: Card[];

    static solve(cards: Card[], game?: string): Hand;
    static winners(hands: Hand[]): Hand[];
  }

  export class CardGroup {
    static fromString(cards: string): CardGroup;
    toString(): string;
    length: number;
    value: string[];
  }
}
</file>

<file path=".gitignore">
node_modules/
client/node_modules/
</file>

<file path="client/index.html">
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Poker MVP React</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/index.tsx"></script>
  </body>
</html>
</file>

<file path="client/package.json">
{
  "name": "poker-client",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.11",
    "typescript": "^5.1.3",
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
</file>

<file path="client/src/App.tsx">
import React, { useEffect, useState } from "react";
import { io, Socket } from "socket.io-client";
import GameControls from "./components/GameControls";
import Table from "./components/Table";

interface Player {
  id: string;
  seat?: number;
  hand: string[];
}

interface GameState {
  seats: (Player | null)[];
  spectators: Player[];
  communityCards: string[];
}

interface ShowdownResult {
  results: any[];
  winners: any[];
}

const socket: Socket = io("http://localhost:3000");

const App: React.FC = () => {
  const [state, setState] = useState<GameState>({
    seats: Array(6).fill(null),
    spectators: [],
    communityCards: [],
  });
  const [showdown, setShowdown] = useState<ShowdownResult | null>(null);
  const [mySeat, setMySeat] = useState<number | null>(null);

  useEffect(() => {
    socket.on("state", (gameState: GameState) => {
      setState(gameState);
      setShowdown(null);
    });

    socket.on("showdown", (result: ShowdownResult) => setShowdown(result));

    socket.on("errorMessage", (msg: string) => alert(msg));

    // 🚀 сразу при подключении запросим актуальное состояние
    socket.emit("getState");

    return () => {
      socket.off("state");
      socket.off("showdown");
      socket.off("errorMessage");
    };
  }, []);

  const handleSit = (seat: number) => {
    socket.emit("join", seat);
    setMySeat(seat);
  };

  return (
    <div
      style={{
        padding: 20,
        fontFamily: "sans-serif",
        color: "#f1f1f1",
        background: "#2e2e2e",
        minHeight: "100vh",
      }}
    >
      <h1 style={{ textAlign: "center" }}>♠️ Poker MVP React</h1>
      <GameControls socket={socket} />

      <Table
        seats={state.seats}
        spectators={state.spectators}
        mySeat={mySeat}
        communityCards={state.communityCards}
        onSit={handleSit}
      />

      <h2>Showdown</h2>
      <pre>{showdown ? JSON.stringify(showdown, null, 2) : "—"}</pre>
    </div>
  );
};

export default App;
</file>

<file path="client/src/components/Card.tsx">
import React from "react";

interface CardProps {
  code?: string; // например "AS", "10H", "KD" или пустая строка
  size?: number; // размер карты в пикселях
  style?: React.CSSProperties;
}

const Card: React.FC<CardProps> = ({ code, size = 60, style }) => {
  // 🔹 путь к картинкам
  const src = code
    ? `/src/assets/cards/${code}.png`
    : `/src/assets/cards/back.png`; // <-- рубашка

  return (
    <img
      src={src}
      alt={code || "back"}
      style={{
        width: size,
        height: size * 1.4, // стандартное соотношение карт
        borderRadius: 6,
        boxShadow: "0 2px 5px rgba(0,0,0,0.5)",
        transition: "transform 0.3s",
        ...style,
      }}
    />
  );
};

export default Card;
</file>

<file path="client/src/components/CommunityCards.tsx">
import React from "react";
import Card from "./Card"; // используем ваш компонент Card

interface CommunityCardsProps {
  cards: string[];
  size?: number;      // размер карты в пикселях
  spacing?: number;   // расстояние между картами
}

const CommunityCards: React.FC<CommunityCardsProps> = ({
  cards,
  size = 60,
  spacing = 10, // уменьшено до 10px
}) => {
  return (
    <div
      style={{
        display: "flex",
        gap: `${spacing}px`,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      {cards.map((code, idx) => (
        <Card key={idx} code={code} size={size} />
      ))}
    </div>
  );
};

export default CommunityCards;
</file>

<file path="client/src/components/GameControls.tsx">
import React from "react";
import { Socket } from "socket.io-client";

interface Props {
  socket: Socket;
}

const GameControls: React.FC<Props> = ({ socket }) => {
  return (
    <div style={{ marginBottom: "20px" }}>
      <button onClick={() => socket.emit("start")}>Start Game</button>
      <button onClick={() => socket.emit("flop")}>Flop</button>
      <button onClick={() => socket.emit("turn")}>Turn</button>
      <button onClick={() => socket.emit("river")}>River</button>
      <button onClick={() => socket.emit("showdown")}>Showdown</button>
      <button onClick={() => socket.emit("reset")}>Reset Game</button>
    </div>
  );
};

export default GameControls;
</file>

<file path="client/src/components/HandDisplay.tsx">
import React from "react";
import Card from "./Card";

interface HandDisplayProps {
  cards: string[];
  title?: string;
  size?: number;    // размер карты в пикселях
  overlap?: number; // сколько пикселей карты накладываются друг на друга
}

const HandDisplay: React.FC<HandDisplayProps> = ({
  cards,
  title,
  size = 60,
  overlap = 20,
}) => {
  const containerHeight = size * 1.4; // высота подложки
  const totalWidth = cards.length > 0 ? size + (cards.length - 1) * (size - overlap) : 0;

  return (
    <div style={{ marginBottom: 20 }}>
      {title && <h3>{title}</h3>}
      <div
        style={{
          position: "relative",
          height: containerHeight,
          width: totalWidth, // ширина подложки равна ширине всей руки
          margin: "0 auto",  // горизонтальное центрирование подложки
        }}
      >
        {cards.map((c, i) => (
          <Card
            key={i}
            code={c}
            size={size}
            style={{
              position: "absolute",
              left: i * (size - overlap),
              top: 0,
            }}
          />
        ))}
      </div>
    </div>
  );
};

export default HandDisplay;
</file>

<file path="client/src/components/SeatsDisplay.tsx">
import React from "react";
import HandDisplay from "./HandDisplay";

interface Player {
  id: string;
  seat?: number; // у наблюдателей может не быть места
  hand: string[];
}

interface SeatsDisplayProps {
  seats: (Player | null)[];
  mySeat: number | null;
  tableWidth: number;
  tableHeight: number;
  seatSize?: number;
  seatOffset?: number;
  onSit: (seat: number) => void;
}

const SeatsDisplay: React.FC<SeatsDisplayProps> = ({
  seats,
  mySeat,
  tableWidth,
  tableHeight,
  seatSize = 120,
  seatOffset = 50,
  onSit,
}) => {
  return (
    <>
      {seats.map((player, i) => {
        const angle = (i / seats.length) * 2 * Math.PI;
        const radiusX = tableWidth / 2 + seatOffset;
        const radiusY = tableHeight / 2 + seatOffset;
        const x = radiusX * Math.cos(angle);
        const y = radiusY * Math.sin(angle);

        const isFree = !player;
        const canSit = isFree && mySeat === null;

        return (
          <div
            key={i}
            style={{
              position: "absolute",
              left: tableWidth / 2 + x - seatSize / 2,
              top: tableHeight / 2 + y - seatSize / 2,
              width: seatSize,
              height: seatSize,
              borderRadius: 12,
              background: canSit ? "#4a7a4a" : isFree ? "#3a5a3a" : "#444",
              border: `2px solid ${canSit ? "#aaffaa" : isFree ? "#777" : "#555"}`,
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              justifyContent: "center",
              color: "white",
              textAlign: "center",
              cursor: canSit ? "pointer" : "default",
              transition: "background 0.2s, border 0.2s",
            }}
            onMouseEnter={(e) => {
              if (canSit) e.currentTarget.style.background = "#5fbf5f";
            }}
            onMouseLeave={(e) => {
              if (canSit) e.currentTarget.style.background = "#4a7a4a";
            }}
            onClick={() => canSit && onSit(i)}
          >
            {player ? (
              <HandDisplay cards={player.hand} title={`Player`} />
            ) : (
              <>
                <div>Место свободно</div>
                {canSit && <div style={{ marginTop: 5 }}>Сесть</div>}
              </>
            )}
          </div>
        );
      })}
    </>
  );
};

export default SeatsDisplay;
</file>

<file path="client/src/components/Table.tsx">
import React from "react";
import SeatsDisplay from "./SeatsDisplay";
import CommunityCards from "./CommunityCards";

interface Player {
  id: string;
  seat?: number; // у наблюдателей места нет
  hand: string[];
}

interface TableProps {
  seats?: (Player | null)[];      // делаем необязательными
  spectators?: Player[];          // делаем необязательными
  mySeat: number | null;
  tableWidth?: number;
  tableHeight?: number;
  seatSize?: number;
  seatOffset?: number;
  communityCards?: string[];      // делаем необязательными
  onSit: (seat: number) => void;
}

const Table: React.FC<TableProps> = ({
  seats = [],                      // дефолт — пустой массив
  spectators = [],                 // дефолт — пустой массив
  mySeat,
  tableWidth = 700,
  tableHeight = 400,
  seatSize = 120,
  seatOffset = 50,
  communityCards = [],             // дефолт — пустой массив
  onSit,
}) => {
  return (
    <>
      <div
        style={{
          position: "relative",
          width: tableWidth,
          height: tableHeight,
          margin: "60px auto",
          borderRadius: "50% / 30%",
          background: "green",
          border: "10px solid brown",
        }}
      >
        {/* Игроки вокруг стола */}
        <SeatsDisplay
          seats={seats}
          mySeat={mySeat}
          tableWidth={tableWidth}
          tableHeight={tableHeight}
          seatSize={seatSize}
          seatOffset={seatOffset}
          onSit={onSit}
        />

        {/* Community Cards в центре стола */}
        <div
          style={{
            position: "absolute",
            top: "50%",
            left: "50%",
            transform: "translate(-50%, -50%)",
          }}
        >
          <CommunityCards cards={communityCards} spacing={10} size={60} />
        </div>
      </div>

      {/* Наблюдатели под столом */}
      <div style={{ marginTop: 20 }}>
        <h3>Наблюдатели</h3>
        <div style={{ display: "flex", gap: 10, flexWrap: "wrap" }}>
          {spectators.map((s) => (
            <div
              key={s.id}
              style={{
                padding: "5px 10px",
                background: "#555",
                borderRadius: 5,
                color: "#fff",
              }}
            >
              {s.id}
            </div>
          ))}
        </div>
      </div>
    </>
  );
};

export default Table;
</file>

<file path="client/src/index.tsx">
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
</file>

<file path="client/tsconfig.json">
{
  "compilerOptions": {
    "target": "ESNext",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"]
}
</file>

<file path="client/vite.config.ts">
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173
  }
});
</file>

<file path="package.json">
{
  "name": "poker-mvp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pokersolver": "^2.1.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "@types/socket.io": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
</file>

<file path="server/Deck.ts">
// server/Deck.ts
// Колода карт и безопасное перемешивание (Fisher-Yates + crypto.randomInt)

import { randomInt } from "crypto";

export default class Deck {
  private cards: string[] = [];

  constructor() {
    const suits = ["s", "h", "d", "c"]; // пики, червы, бубны, трефы
    const ranks = ["2", "3", "4", "5", "6", "7", "8", "9", "T", "J", "Q", "K", "A"]; // T = Ten
    this.cards = [];
    for (const suit of suits) {
      for (const rank of ranks) {
        this.cards.push(`${rank}${suit}`); // пример: "As", "Td"
      }
    }
    this.shuffle();
  }

  // Криптографически более надёжное тасование
  shuffle() {
    for (let i = this.cards.length - 1; i > 0; i--) {
      const j = randomInt(0, i + 1);
      [this.cards[i], this.cards[j]] = [this.cards[j], this.cards[i]];
    }
  }

  // Раздать n карт — удаляются из колоды
  deal(n: number): string[] {
    return this.cards.splice(0, n);
  }
}
</file>

<file path="server/Game.ts">
import Deck from "./Deck.js";
import pkg from "pokersolver";
const { Hand } = pkg;

export interface Player {
  id: string;
  seat: number;
  hand: string[];
  chips: number;           // стек игрока
  bet: number;             // текущая ставка в раунде
  folded: boolean;         // сбросил карты
  allIn: boolean;          // пошел ва-банк
}

export interface GameState {
  seats: (Player | null)[];
  spectators: { id: string }[];
  communityCards: string[];
  pot: number;
  currentBet: number;
  currentPlayer: number | null;
  dealerPosition: number;
  smallBlind: number;
  bigBlind: number;
  stage: 'waiting' | 'preflop' | 'flop' | 'turn' | 'river' | 'showdown';
}

export default class Game {
  private seats: (Player | null)[] = Array(6).fill(null);
  private spectators: { id: string }[] = [];
  private communityCards: string[] = [];
  private deck: Deck | null = null;
  
  private pot: number = 0;
  private currentBet: number = 0;
  private currentPlayer: number | null = null;
  private dealerPosition: number = 0;
  private smallBlind: number = 10;
  private bigBlind: number = 20;
  private stage: GameState['stage'] = 'waiting';
  private lastRaisePosition: number | null = null;

  // Добавление игрока
  addPlayer(id: string, seat: number, chips: number = 1000): boolean {
    if (seat < 0 || seat >= this.seats.length) return false;
    if (this.seats[seat]) return false;
    if (this.stage !== 'waiting') return false; // нельзя добавлять во время игры

    this.spectators = this.spectators.filter((p) => p.id !== id);

    const player: Player = {
      id,
      seat,
      hand: [],
      chips,
      bet: 0,
      folded: false,
      allIn: false,
    };
    this.seats[seat] = player;
    return true;
  }

  addSpectator(id: string): boolean {
    if (!this.spectators.find((p) => p.id === id)) {
      this.spectators.push({ id });
    }
    return true;
  }

  removePlayer(id: string) {
    this.seats = this.seats.map((p) => (p?.id === id ? null : p));
    this.spectators = this.spectators.filter((p) => p.id !== id);
  }

  reset() {
    this.deck = null;
    this.communityCards = [];
    this.pot = 0;
    this.currentBet = 0;
    this.currentPlayer = null;
    this.stage = 'waiting';
    this.lastRaisePosition = null;
    
    this.seats.forEach((p) => {
      if (p) {
        p.hand = [];
        p.bet = 0;
        p.folded = false;
        p.allIn = false;
      }
    });
  }

  start() {
    const activePlayers = this.getActivePlayers();
    if (activePlayers.length < 2) {
      throw new Error("Нужно минимум 2 игрока");
    }

    this.reset();
    this.deck = new Deck();
    this.deck.shuffle();
    this.stage = 'preflop';

    // Раздача карт
    this.seats.forEach((p) => {
      if (p && p.chips > 0) {
        p.hand = this.deck!.deal(2);
      }
    });

    // Установка блайндов
    this.postBlinds();
    
    // Первый игрок после большого блайнда
    this.currentPlayer = this.getNextPlayer(this.getBigBlindPosition());
  }

  private postBlinds() {
    const sbPos = this.getSmallBlindPosition();
    const bbPos = this.getBigBlindPosition();

    const sbPlayer = this.seats[sbPos];
    const bbPlayer = this.seats[bbPos];

    if (sbPlayer) {
      const sbAmount = Math.min(this.smallBlind, sbPlayer.chips);
      sbPlayer.bet = sbAmount;
      sbPlayer.chips -= sbAmount;
      this.pot += sbAmount;
      if (sbPlayer.chips === 0) sbPlayer.allIn = true;
    }

    if (bbPlayer) {
      const bbAmount = Math.min(this.bigBlind, bbPlayer.chips);
      bbPlayer.bet = bbAmount;
      bbPlayer.chips -= bbAmount;
      this.pot += bbAmount;
      this.currentBet = bbAmount;
      if (bbPlayer.chips === 0) bbPlayer.allIn = true;
    }

    this.lastRaisePosition = bbPos;
  }

  // Действия игрока
  fold(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    player.folded = true;
    this.nextPlayer();
    return true;
  }

  check(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;
    if (player.bet < this.currentBet) return false; // нельзя чекать, нужно коллировать

    this.nextPlayer();
    return true;
  }

  call(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const toCall = this.currentBet - player.bet;
    const actualBet = Math.min(toCall, player.chips);
    
    player.bet += actualBet;
    player.chips -= actualBet;
    this.pot += actualBet;

    if (player.chips === 0) player.allIn = true;

    this.nextPlayer();
    return true;
  }

  raise(playerId: string, amount: number): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const toCall = this.currentBet - player.bet;
    const totalBet = toCall + amount;

    if (totalBet > player.chips) return false; // недостаточно фишек
    if (amount < this.bigBlind) return false; // минимальный рейз = BB

    player.chips -= totalBet;
    player.bet += totalBet;
    this.pot += totalBet;
    this.currentBet = player.bet;

    if (player.chips === 0) player.allIn = true;

    this.lastRaisePosition = player.seat;
    this.nextPlayer();
    return true;
  }

  allIn(playerId: string): boolean {
    const player = this.getCurrentPlayerIfValid(playerId);
    if (!player) return false;

    const allInAmount = player.chips;
    player.bet += allInAmount;
    player.chips = 0;
    this.pot += allInAmount;
    player.allIn = true;

    if (player.bet > this.currentBet) {
      this.currentBet = player.bet;
      this.lastRaisePosition = player.seat;
    }

    this.nextPlayer();
    return true;
  }

  // Переход к следующему игроку или следующей стадии
  private nextPlayer() {
    const next = this.getNextPlayer(this.currentPlayer!);
    
    // Проверка: все ли уравняли ставки
    if (this.isBettingRoundComplete()) {
      this.nextStage();
    } else {
      this.currentPlayer = next;
    }
  }

  private isBettingRoundComplete(): boolean {
    const activePlayers = this.getActivePlayers();
    
    // Все игроки (кроме all-in) сделали ход и уравняли ставку
    const playersWhoCanAct = activePlayers.filter(p => !p.allIn);
    
    if (playersWhoCanAct.length === 0) return true;
    
    const allBetsEqual = playersWhoCanAct.every(p => p.bet === this.currentBet);
    const allActed = this.currentPlayer === this.lastRaisePosition || playersWhoCanAct.length === 1;
    
    return allBetsEqual && allActed;
  }

  private nextStage() {
    // Сброс ставок игроков
    this.seats.forEach(p => {
      if (p) p.bet = 0;
    });
    this.currentBet = 0;

    const activePlayers = this.getActivePlayers();
    
    if (activePlayers.length === 1) {
      // Все сбросили, победитель забирает банк
      this.awards([activePlayers[0]]);
      return;
    }

    switch (this.stage) {
      case 'preflop':
        this.flop();
        this.stage = 'flop';
        break;
      case 'flop':
        this.turn();
        this.stage = 'turn';
        break;
      case 'turn':
        this.river();
        this.stage = 'river';
        break;
      case 'river':
        this.stage = 'showdown';
        this.showdown();
        return;
    }

    this.currentPlayer = this.getNextPlayer(this.dealerPosition);
    this.lastRaisePosition = this.currentPlayer;
  }

  flop() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(3));
  }

  turn() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(1));
  }

  river() {
    if (!this.deck) return;
    this.communityCards.push(...this.deck.deal(1));
  }

  showdown() {
    const activePlayers = this.getActivePlayers();

    const results = activePlayers.map((p) => {
      const full = [...p.hand, ...this.communityCards];
      const solved = Hand.solve(full);
      return {
        player: p,
        hand: solved,
        descr: solved.descr,
        rank: solved.rank,
      };
    });

    const winnerHands = Hand.winners(results.map(r => r.hand));
    const winners = results.filter(r => winnerHands.includes(r.hand));

    this.awards(winners.map(w => w.player));

    return {
      results: results.map(r => ({
        id: r.player.id,
        seat: r.player.seat,
        hand: r.player.hand,
        descr: r.descr,
        rank: r.rank,
      })),
      winners: winners.map(w => ({
        id: w.player.id,
        descr: w.descr,
      })),
    };
  }

  private awards(winners: Player[]) {
    const share = Math.floor(this.pot / winners.length);
    winners.forEach(w => {
      w.chips += share;
    });
    this.pot = 0;
    this.stage = 'waiting';
    this.currentPlayer = null;
    
    // Передвижение дилера
    this.dealerPosition = this.getNextSeat(this.dealerPosition);
  }

  // Вспомогательные методы
  private getCurrentPlayerIfValid(playerId: string): Player | null {
    if (this.currentPlayer === null) return null;
    const player = this.seats[this.currentPlayer];
    if (!player || player.id !== playerId || player.folded || player.allIn) {
      return null;
    }
    return player;
  }

  private getActivePlayers(): Player[] {
    return this.seats.filter((p): p is Player => 
      p !== null && !p.folded
    );
  }

  private getNextPlayer(fromSeat: number): number {
    let seat = this.getNextSeat(fromSeat);
    let attempts = 0;
    
    while (attempts < this.seats.length) {
      const player = this.seats[seat];
      if (player && !player.folded && !player.allIn && player.chips > 0) {
        return seat;
      }
      seat = this.getNextSeat(seat);
      attempts++;
    }
    
    return fromSeat;
  }

  private getNextSeat(fromSeat: number): number {
    let seat = (fromSeat + 1) % this.seats.length;
    let attempts = 0;
    
    while (attempts < this.seats.length) {
      if (this.seats[seat] !== null) {
        return seat;
      }
      seat = (seat + 1) % this.seats.length;
      attempts++;
    }
    
    return fromSeat;
  }

  private getSmallBlindPosition(): number {
    return this.getNextSeat(this.dealerPosition);
  }

  private getBigBlindPosition(): number {
    return this.getNextSeat(this.getSmallBlindPosition());
  }

  getState(): GameState {
    return {
      seats: this.seats,
      spectators: this.spectators,
      communityCards: this.communityCards,
      pot: this.pot,
      currentBet: this.currentBet,
      currentPlayer: this.currentPlayer,
      dealerPosition: this.dealerPosition,
      smallBlind: this.smallBlind,
      bigBlind: this.bigBlind,
      stage: this.stage,
    };
  }

  // Получить состояние для конкретного игрока (скрыть карты других)
  getStateForPlayer(playerId: string): any {
    const state = this.getState();
    return {
      ...state,
      seats: state.seats.map(p => {
        if (!p) return null;
        if (p.id === playerId || this.stage === 'showdown') {
          return p; // показываем карты
        }
        return { ...p, hand: [] }; // скрываем карты
      }),
    };
  }

  setBlinds(small: number, big: number) {
    if (this.stage !== 'waiting') return false;
    this.smallBlind = small;
    this.bigBlind = big;
    return true;
  }
}
</file>

<file path="server/index.ts">
import express from "express";
import http from "http";
import { Server } from "socket.io";
import Game from "./Game.js";

const app = express();
const server = http.createServer(app);
const io = new Server(server, {
  cors: { origin: "*" },
});

const PORT = 3000;
const game = new Game();

app.get("/", (_req, res) => {
  res.send("Poker server is running");
});

io.on("connection", (socket) => {
  console.log("Player connected:", socket.id);

 const updateState = function () {
    let state = game.getState()
    let s = structuredClone(state)
    let players: string[] = [];
    s.seats.filter((p) => p !== null).map((p) => {
      if (p.id !== null ) {
        players.push(p.id)
      }
    })
    s.spectators.map((p) => {
      if (p.id !== null ) {
        players.push(p.id)
      }
    })

    players.forEach((id) => {
      let playerSeats: any[] = []
      s.seats.map((pl) => {
        let p = structuredClone(pl)
        if (p !== null && p.id !== id && p.hand.length > 0) {
          p.hand = ["back","back"]
        }
        playerSeats.push(p)
      })
      let playerState = {
        seats: playerSeats,
        spectators: s.spectators,
        communityCards: s.communityCards,
      }
      console.log(`Sending state to id# ${id}...`)
      console.log(playerState)
      io.to(id).emit("state", playerState )
    })
    
  }

  // Новый клиент по умолчанию становится наблюдателем
  game.addSpectator(socket.id);

  // Клиент сам может запросить состояние
  socket.on("getState", () => {
    updateState();
  });

  // Игрок хочет занять место
  socket.on("join", (seat: number) => {
    const success = game.addPlayer(socket.id, seat);
    if (!success) {
      socket.emit("errorMessage", "Место занято или неверный номер");
      return;
    }
    updateState();
  });

  socket.on("reset", () => {
    game.reset();
    updateState();
  });

  socket.on("start", () => {
    game.start();
    updateState();
  });

  socket.on("flop", () => {
    game.flop();
    updateState();
  });

  socket.on("turn", () => {
    game.turn();
    updateState();
  });

  socket.on("river", () => {
    game.river();
    updateState();
  });

  socket.on("showdown", () => {
    const result = game.showdown();
    io.emit("state", game.getState())
    io.emit("showdown", result);
  });

  socket.on("disconnect", () => {
    console.log("Player disconnected:", socket.id);
    game.removePlayer(socket.id);
    updateState();
  });
});

server.listen(PORT, () => {
  console.log(`Poker server running on http://localhost:${PORT}`);
});
</file>

<file path="server/types.ts">
export type Player = {
  id: string;
  hand: string[];
};
</file>

<file path="ts-node-esm.js">
#!/usr/bin/env node
import { register } from "ts-node";
import { pathToFileURL } from "node:url";

register({
  transpileOnly: true,
  compilerOptions: {
    module: "NodeNext",
    target: "ES2022"
  }
});

import(pathToFileURL("./server/index.ts").href);
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    /* Основные параметры */
    "target": "ES2022",                       /* Современный синтаксис (под Node 18+) */
    "module": "NodeNext",                     /* Поддержка ESM */
    "moduleResolution": "NodeNext",           /* Поиск модулей как в ESM */
    "rootDir": "./server",                    /* Где исходники TypeScript */
    "outDir": "./dist",                       /* Куда собирать JS */

    /* Типизация */
    "strict": true,                           /* Строгий режим */
    "esModuleInterop": true,                  /* Упрощает работу с CommonJS модулями */
    "forceConsistentCasingInFileNames": true,

    /* Поддержка кастомных типов */
    "typeRoots": ["./types", "./node_modules/@types"],

    /* Удобства */
    "skipLibCheck": true                      /* Не проверять типы в node_modules */
  },
  "include": ["server", "types"],             /* Берём исходники и типы */
  "exclude": ["node_modules", "dist"]         /* Игнорируем мусор */
}
</file>

<file path="types/pokersolver.d.ts">
declare module "pokersolver" {
  export type Card = string;

  export class Hand {
    name: string;
    descr: string;
    rank: number;
    cards: Card[];

    static solve(cards: Card[], game?: string): Hand;
    static winners(hands: Hand[]): Hand[];
  }

  export class CardGroup {
    static fromString(cards: string): CardGroup;
    toString(): string;
    length: number;
    value: string[];
  }
}
</file>

</files>
